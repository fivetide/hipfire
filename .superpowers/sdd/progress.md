> **Historical / superseded:** This is a chronological, append-only SDD ledger retained as historical context, not current status. Older entries may contradict newer entries. See the [authoritative device-mesh refactor tracker](../../.agent-progress/device-mesh-refactor-tracker.md).

# Daemon god-struct collapse (ArchDispatch) — Inc 0 + Inc 1 — SDD ledger
Plan: docs/superpowers/plans/2026-07-09-daemon-god-struct-archdispatch-inc0-inc1.md
Spec: docs/superpowers/specs/2026-07-09-daemon-god-struct-archdispatch-design.md
Branch base (this refactor): 694d1b81
Worktree: /home/bjoern/hipfire/.claude/worktrees/feature+device-mesh (branch feature/device-mesh; NO new branch)
Validation split: implementers do NO-GPU work (build + lib tests + commit); CONTROLLER runs GPU gates IN-SESSION (GPU+models confirmed available 2026-07-09) via scripts/gpu-lock.sh — coherence-gate.sh, serve-multiturn-gate.sh, dual-run parity (HIPFIRE_ARCHDISPATCH_PARITY=1 HIPFIRE_DETERMINISTIC=1, FP32 DeltaNet state per byte-parity rule) on qwen3.5-9b.mq4. Models: ~/.hipfire/models/qwen3.5-{0.8b,4b,9b}.mq4 present.

## VALIDATION MATRIX (bjoern 2026-07-09, HARD gate on Tasks 1.3 & 1.5) — SINGLE-GPU **and** DUAL-GPU EMULATION
Every Inc-1 GPU validation runs BOTH: (a) single physical GPU, (b) `HIPFIRE_EMULATE_GPUS=2` (device-mesh convention: byte-identical single vs emulated-2).
- WHY it bites here: `reset_qwen35_recurrent` has a `m.pp>1` multi-GPU branch (per-LA-layer memsets via `pp_dn_la_to_device`) ≠ the single-GPU branch. Task 1.3's `model_reset_context` wraps it → must validate reset on the emulated Pp-2 arm too. NOTE the embedded qwen35 arm Task 1.4 extracts is the SINGLE-GPU path (arch 5/6 fallthrough); the pp>1 qwen35 path is `generate_multi` (separate, NOT migrated in Inc 1) — but its RESET still funnels through model_reset_context, so Pp-2 serve-multiturn is required.
- Task 1.3: serve-multiturn (state-bleed) on single-GPU AND emulated Pp-2 (pp>1 qwen35 load).
- Task 1.5: coherence + serve-multiturn + dual-run parity on single-GPU AND HIPFIRE_EMULATE_GPUS=2.
Byte-identical is the bar. Worktree core.hooksPath=.git/hooks → gates do NOT auto-fire; run manually.

## Tasks
- Task 0.1 (ArchDispatch trait + SamplingDefaults/ArchFeatures): COMPLETE (commit bdf9d678, review SPEC ✅ / quality Approved, 0 issues). fmt-churn reorder caught+reverted (31382a69→bdf9d678).
- Task 0.2 (SessionState): COMPLETE (commits bb86ad85→c02696ed→3c8cefaf; review SPEC ✅ / Approved). CONCRETE SessionState in hipfire-loader; clear() drains via DeltaNetSnapshot::free_gpu (no Drop → explicit free, mirrors daemon free_checkpoints). 2 Minors fixed in 3c8cefaf (runtime lib.rs mod-reorder reverted; _gpu→gpu).
- Task 0.3 (ModelParallelKind classifier): COMPLETE (commit 1c78d297, review SPEC ✅ / Approved). Real precedence verified tp@6891>pp_dense@6940>ep@6990>pp_qwen35@7374>single; in hipfire-loader. Minors non-blocking (no multi-flag assert; SPDX header = repo convention).
- Task 0.4 (Inc 0 integration gate): COMPLETE. cargo build --workspace --all-targets --locked GREEN (exit 0, only pre-existing warnings); cargo test --lib --workspace GREEN (all pass incl arch_dispatch/session_state/model_parallel). No code change (allow-attrs already in prior tasks). INC 0 DONE @1c78d297.
- Task 1.1 (dual-run shadow-parity harness): COMPLETE (commits 0000b1a4→7de523dd; CONTROLLER-reviewed — 2 sonnet reviewer subagents died mid-run, reviewed the 69-line additions-only diff myself: SPEC ✅ / Approved). Harness + first_div-boundary fix; self-check OK, seeded-divergence panic verified. No churn.
- Task 1.2 (qwen35 ArchDispatch arch-invariant + reset): COMPLETE (commit f40463e9; controller-reviewed SPEC ✅/Approved, build clean). Delegations verified: reset→reset_qwen35_recurrent@4193; eos→b.config.eos_token; sampling=(0.3,0.8,1.0) confirmed vs ladder@1964/1994/2038; features think/stop/grammar=T,vision=F. as_spec_target=None+TODO(1.4).
- Task 1.3 (reset collapse): CODE COMPLETE + controller-reviewed byte-identical (commits e6dcd195 add fn + f959fdfe fold reset-cmd). model_reset_context = reset-cmd handler op-set verbatim (same order); fold removes exactly that block. 1 site folded, 3 skipped (VL-guard subset/comment-drift; 2 bench subsets). Build clean. GPU-VALIDATED (controller, targeted reset session load→gen→RESET→gen→unload on qwen3.5-4b.mq4):
    - SINGLE-GPU: reset fired seq_pos=0, post-reset switched France→Japan, ZERO bleed, no panic. CONFIRMED.
    - EMULATED-2 (HIPFIRE_EMULATE_GPUS=2 + params.pp:2): loaded+reset+generated clean, no bleed, no panic (per-stage band logs not surfaced w/o HIPFIRE_PP_LAYERS → uniform banding; pp=2 topo not log-confirmed).
    - Structural: Task 1.3 does NOT touch reset_qwen35_recurrent (pp>1 branch byte-unchanged); model_reset_context calls it identically → multi-GPU reset preserved by construction.
  TODO before 1.5: nail down exact pp=2 qwen35 emulated invocation + a log/assert that confirms pp banding engaged (matters for ar_generate in 1.4).
  Task 1.3 COMPLETE.
- Task 1.4 (ar_generate + qwen35 AR hooks, dual-run gated): IN PLANNING [GPU]. MAP DONE (agent aad97801): qwen35 AR arm = lines 8706-9850 (1145 lines, HEAD f959fdfe), 5 arch hooks (prefill_forward@8764/8792=forward_prefill_batch; decode_step_forward@9223/9365/9570/9727=forward_scratch; init_grammar@8977=Matcher::new; apply_grammar_mask@9040/9501/9649; grammar_advance@9053/9668). Committed-token emit = SINGLE site @9180 (emit_committed_event) → TokenTape. Tangled-but-arch-NEUTRAL (driver owns, NOT hooks): think-cap(9291-9480), budget-inject(9497-9580), n-gram(9482-9495), DeltaNet checkpoints(8824/9225), adaptive-KV(8835/9241), eviction(8805/9241), LCP(8200-8270). Parity reset = model_reset_context (8 fields; asst_turn_cache+decoded_vocab persist).
  DECISIONS (bjoern 2026-07-09): (1) scope = AR-arm ONLY (spec/DFlash stays dispatched upstream, ar_generate never sees spec, as_spec_target unused). (2) grammar API = OPTION B: `trait GrammarMatcher{token_mask/advance/is_free}` in runtime arch_dispatch.rs + `ArchDispatch::init_grammar(&self,tool_schemas)->Option<Box<dyn GrammarMatcher>>`; ORPHAN-RULE fix = newtype `Qwen35GrammarMatcher(qwen35::grammar::Matcher)` in daemon.rs (daemon owns neither trait nor Matcher → can't impl directly; runtime-lib can't name qwen35 type = dep cycle). apply_mask_to_logits stays the generic sampler call.
  SUB-INCREMENTS: 1.4a DONE (commit 5592f8be, controller SPEC ✅/Approved, build clean): GrammarMatcher trait (token_mask/advance/is_free, primitive types) + 3 hooks (prefill_forward→forward_prefill_batch, decode_step_forward→forward_scratch, init_grammar→Some(Box::new(Qwen35GrammarMatcher(Matcher::new)))) w/ Err/None defaults; newtype Qwen35GrammarMatcher in daemon (orphan-safe); init_grammar takes &[(String,Vec<String>)] (ToolSchema cycle-avoid, daemon converts). Additions-only (+ harmless test-body rustfmt in arch_dispatch.rs). 1.4b ar_generate driver = arm body moved to fn w/ 5 calls routed through hooks, tangle stays in driver (not routed yet); 1.4c wire dual-run parity + route qwen35 through ar_generate under HIPFIRE_ARCHDISPATCH_PARITY=1 only; 1.4d controller GPU-validate parity single+emulated-2 → flip default + delete old arm.

  === 1.4b RE-SCOPE (2026-07-10, fresh session; map agent ae3cec15 + layering analysis) ===
  MAP CORRECTION: post-1.4a arm = daemon.rs 8791–9935 (guard `if m.arch_id==5||6`) inside `fn generate`(→`()`); depends on a SHARED PREAMBLE 7885–8790 (also feeds the llama else-arm) that renders `new_tokens`, im_end/nl, im_end_token/tool_call_pair/think_pair, prefill_tokens, cached_tokens_count, pflash_*, t0, tokenizer. Arm EXITS: bare `return;` at 8953 (prefill-abort) + 9260 (decode-abort); all other loop control is `break` → finalize 9763–9935. `TokenTape` DOES exist (daemon.rs:667, `assert_token_parity`). No live callers of any 1.4a hook / Qwen35Dispatch (rework fully contained to trait+impl).
  PLAN-INVALIDATING FINDING: the plan's "route 5 hooks, tangle stays in a GENERIC driver" is INFEASIBLE as written. (1) The "arch-neutral tangle" (eviction/adaptive-KV/checkpoint/abort-zero) operates on `&mut b.kv_cache`/`&mut b.dn_state` (INSIDE the arch bundle) → a generic driver can't name `ModelState::Qwen35` → these need hooks too (not just the 5 forward/grammar sites). (2) CRATE LAYERING: `LoadedModel`∈hipfire-loader, which DEPENDS ON hipfire-runtime (loader→runtime normal dep; runtime→loader is DEV-only for the daemon example) ⇒ the ArchDispatch trait (runtime lib) CANNOT name `LoadedModel`/`DeltaNetState`/`ForwardScratch` (arch types). So a "stateless dispatcher takes m as param" is impossible; the dispatcher MUST own `&mut LoadedModel` (as Task 1.2's `Qwen35Dispatch<'m>{m}` already does).
  bjoern DECISION (2026-07-10): "Genericize now (expand hooks)" — NOT a hollow qwen35-specific relocation. Feasible shape given layering: dispatcher OWNS `&mut m` (keep 1.2 model, hooks stay `&mut self`, field-split m internally); ar_generate holds `dispatch: &mut dyn ArchDispatch` (constructed by caller in 1.4c, dead-code in 1.4b) + keeps ALL loop state as LOCALS (seq_pos, streamed_tokens, generated, think/budget counters, filter, rng) so it needs almost nothing back from m during the loop. KEY simplifications: ngram_scope == `&streamed_tokens` (== `m.conversation_tokens[start..]`, proven same push order) so no conversation_tokens read in-loop; seq_pos is a driver local passed to every hook, synced to m at finalize. Runtime-nameable trait types OK: KvCache, SamplerConfig, Tokenizer, GrammarMatcher.
  EXPANDED HOOK SURFACE (all `&mut self`/`&self`, self owns m; verbatim bodies from arm): keep prefill_forward/decode_step_forward/init_grammar/eos_token/sampling_defaults/features/reset; ADD maybe_evict(→Option<new_physical>), maybe_adaptive_downshift(eprintln inside), take_prefill_checkpoint, abort_zero_recurrent(zero dn s_matrices/s_scales/conv_states + kv.compact_offset + llama kv), sample(&self,gpu,cfg:&SamplerConfig,vocab,ngram_scope,mask:Option<&[bool]>,rng)->u32 (GPU path / CPU-mask path). ADD `attractor_detected(&self)->bool` to GrammarMatcher (site 9751). Driver-field accessors (tokenizer/conversation-sync/asst_turn_cache-finalize/seq_pos-writeback) added in the driver stage as needed (additive default methods). Abort/finalize event emit + seq_pos=0/conv.clear/free_checkpoints stay DRIVER-generic (only the recurrent-state zeroing is the hook).
  REVISED SUB-PLAN: 1.4b-i = expanded ArchDispatch trait + GrammarMatcher::attractor_detected (runtime lib; default impls; build lib). 1.4b-ii = Qwen35Dispatch impl of new hooks, verbatim from arm (daemon; build daemon; dead-code). 1.4b-iii = ar_generate generic driver routing through hooks, #[allow(dead_code)], NOT routed at dispatch point (build daemon). Then 1.4c/1.4d unchanged. Each stage additive + build-verified; faithfulness proven by 1.4c parity harness on GPU.
  STATUS (2026-07-10): 1.4b-i DONE (commit 380e7bea — 6 methods added: maybe_evict/maybe_adaptive_downshift/take_prefill_checkpoint/abort_zero_recurrent/sample + GrammarMatcher::attractor_detected; all default no-op/fail-loud; lib builds). 1.4b-ii DONE (commit 036f07cf — Qwen35Dispatch impls verbatim from arm, each reproducing the `let m=&mut *self.m;` field-split; fixed sample rng u64→u32; daemon builds + 336 lib tests pass). NOTE fmt-changed.sh is a FOOTGUN in this worktree: it reformats the ENTIRE feature/device-mesh delta-vs-master (~16 files incl daemon.rs), re-creating stashed churn — do NOT run it; the stray-rustfmt churn is parked in `git stash@{0}` ("stray rustfmt churn (15 files...)"). Only rustfmt the single file you edit if needed.
  1.4b-iii SPEC (NEXT — the ~1145-line generic driver; fresh-context recommended): add `#[allow(dead_code)] fn ar_generate(dispatch: &mut dyn ArchDispatch, gpu, stdout, id, <sampling params>, new_tokens: Vec<u32>, im_end/nl/im_end_token/tool_call_pair/think_pair, prefill_tokens, cached_tokens_count, pflash_summary/bypass_reason/alpha, t0, tape: Option<&mut TokenTape>)` in daemon.rs. Body = the arm (daemon.rs 8791–9935) reproduced with: (a) loop state as LOCALS (seq_pos, streamed_tokens, generated, think/budget counters, filter, rng:u32); ngram_scope = `&streamed_tokens` (proven == m.conversation_tokens[start..]); (b) EVERY arch site → hook: forward_prefill_batch→dispatch.prefill_forward; forward_scratch→decode_step_forward; ev.maybe_evict→maybe_evict (returns Option<new_physical>→update local seq_pos); ad.maybe_downshift→maybe_adaptive_downshift; take_dn_checkpoint→(gate `if ckpt_resume_enabled()`)→take_prefill_checkpoint; abort dn/kv zero→abort_zero_recurrent (driver still owns seq_pos=0/conv.clear/free_checkpoints/event-emit/return); Matcher::new→dispatch.init_grammar; the 3 sample sites→dispatch.sample(gpu,&cfg,vocab,ngram_scope,mask,&mut rng) where driver computes mask via the Box<dyn GrammarMatcher> (token_mask) when grammar_active && !is_free; grammar advance/attractor via the boxed matcher; config.eos_token→dispatch.eos_token(). Driver-field accessors still needed (add as additive-default trait methods when hit): tokenizer (re-fetch per use: add `fn tokenizer(&self)->&Tokenizer`), conversation_tokens push/extend/clear + seq_pos writeback + asst_turn_cache finalize (add small accessors or a `finalize_turn` hook). NOT routed at the 8791 dispatch point (dead code). Build daemon. UNSEEN arm regions to read for iii: grammar setup 9020–9095, think-cap splice 9366–9505, budget splice 9524–9691, chatml-trailer+finalize 9700–9935 (each splice re-uses the same hooks). Map detail: agent ae3cec15 report (in this session's context) — captured-locals table + hook-site line numbers + borrow hazards.
  1.4b-iii DONE (commit e1d1b97a — daemon builds + lib builds + 336 lib tests pass; only daemon.rs + arch_dispatch.rs changed). ar_generate(dispatch:&mut dyn ArchDispatch, gpu, stdout, id, temp/top_p/top_k/min_p/max_tokens/repeat_penalty/repeat_window/presence_penalty/frequency_penalty/budget_alert_at_tok/budget_alert_text/max_think_tokens/assistant_prefix/stop, tools:Option<&[Value]>, new_tokens:Vec<u32>, im_end/nl/im_end_token/tool_call_pair/think_pair, prefill_tokens, cached_tokens_count, pflash_summary/bypass_reason/alpha, t0, tape:Option<&mut TokenTape>). ADDED (beyond 1.4b-i) 13 driver-accessor trait methods (default unimplemented!/no-op, impl'd on Qwen35Dispatch): tokenizer, seq_pos, set_seq_pos, conversation_tokens_mut, vocab_size, repeat_buf_cap_bytes, prefill_max_batch, free_prefill_checkpoints, ensure_decoded_vocab, has_eviction, physical_cap, eviction_window, insert_asst_turn. (Note: `tools` param added — NOT in the pre-spec; the JSON tool-schema→pairs extraction is arch-neutral, kept in driver.) asst_turn_cache finalize handled inline: reads real conversation_tokens via conversation_tokens_mut()[decode_start..] (includes post-loop ChatML trailer — correct), fp via existing free fns strip_think_for_fingerprint/maybe_normalize_prompt/asst_turn_fingerprint, store via insert_asst_turn hook. pflash_done_fragment duplicated as a nested fn inside ar_generate (generate's copy untouched).
  *** 1.4c PARITY-REVIEW FLAGS (faithfulness deviations — verify token-identity handles these) ***: (1) ngram_scope uses local `streamed_tokens` in place of `m.conversation_tokens[ngram_scope_start..]` — provably same push-order at all 3 in-loop sample sites (main/think-close/nudge push to BOTH; trailer is post-loop); tok0 uses `&[]` (arm's is empty there). (2) adaptive-KV eprintln phase label unified (prefill/post-prefill/decode → one msg) — stderr diagnostic only, not token-affecting. (3) abort blocks: the independent state-zeroing ops (abort_zero_recurrent vs seq_pos=0/conv.clear/free_checkpoints) are reordered vs the arm between the prefill-abort and decode-abort sites — order-independent (disjoint state), but confirm. (4) seq_pos is a local written back once via set_seq_pos(seq_pos) at finalize + set_seq_pos(0) on abort (arm mutates m.seq_pos per-token) — net-equal end state. NEXT: 1.4c = wire dual-run parity harness (HIPFIRE_ARCHDISPATCH_PARITY=1) constructing Qwen35Dispatch{m} and calling ar_generate on a reset context, assert_token_parity vs old arm; then 1.4d GPU-validate single + emulated-2, flip default, delete old arm.
- Task 1.4c DONE (commit 8a6b5dc7): dual-run shadow-parity wiring at the qwen35 dispatch (9755). Under HIPFIRE_ARCHDISPATCH_PARITY=1 the legacy arm captures committed tokens into __old_tape (3 sites: main/think-cap/budget, mirroring ar_generate's tape pushes), then model_reset_context + re-run ar_generate into __new_tape + assert_token_parity. Flag off = legacy arm only (3 no-op checks), zero behavior change. Build: daemon + workspace clean.
- Task 1.4d-a (GPU parity VALIDATION) DONE + PASSED (controller, 2026-07-10): release daemon, qwen3.5-4b.mq4, state_quant=fp32 (stderr confirms "DeltaNet state: FP32") + HIPFIRE_DETERMINISTIC=1 + HIPFIRE_ARCHDISPATCH_PARITY=1, greedy temp0 max72. PARITY HELD (assert_token_parity did NOT fire; 2 done events = both arms ran) on 5 prompts × 2 configs: SINGLE-GPU {capital/fibonacci-code/sky-reasoning/primes-list} + EMULATED-2 (HIPFIRE_EMULATE_GPUS=2) {fibonacci}. All 72 tokens (full decode, non-empty tapes). ar_generate == legacy arm on prefill+main-loop+finalize+eos+grammar-free-sample. The 4 flagged deviations (ngram_scope=streamed_tokens, seq_pos writeback, abort reorder, eprintln label) caused ZERO divergence.
  COVERAGE GAPS (not parity-exercised — reuse the SAME proven hooks, but untested): think-cap force-close splice (needs max_think cap hit), budget-alert splice, tool-call GRAMMAR path (needs tools=), long-context adaptive-KV/eviction (parity reset limits to short single-turn prompts).
- Task 1.4d-b (FLIP default + DELETE old arm) DONE (commit 0e1ebc2d): replaced the arm body with `Qwen35Dispatch{m:&mut *m}` + `ar_generate(..,None)` (tape=None, NO reset in prod; seq_pos seeded from dispatch.seq_pos() for multi-turn continuation), deleted −1212 legacy lines + the 1.4c parity scaffold. ar_generate is now the SOLE qwen35 AR serving path. Post-flip validation (q8 prod config): coherence-gate PASS (cap/code/reason/**tool-call-grammar**/mq3/mq4/mq6/lloyd, 0.8b-27b — all fluent, no attractors; the grammar/tool-call path that parity didn't exercise is confirmed coherent here); serve-multiturn-gate PASS (no cross-request bleed). Combined coherence exit 1 = pflash perf-stage gfx1100-baseline-vs-gfx1151 artifact (uniform ~2× incl untouched _baseline rows, 27b spec path NOT through ar_generate) — the documented env artifact, NOT this change.
=== INC 1 TASK 1.4 COMPLETE (1.4a→1.4d). qwen35 (arch 5/6) fully migrated to ArchDispatch + ar_generate; legacy embedded arm gone. Commits: 5592f8be(1.4a) 380e7bea 036f07cf e1d1b97a(1.4b) 8a6b5dc7(1.4c) 0e1ebc2d(1.4d). ===
- Task 1.5 (Inc 1 validation gate): LARGELY SATISFIED by the 1.4d validation — dual-run parity token-identical on single-GPU AND HIPFIRE_EMULATE_GPUS=2 (5 prompts, fp32 DeltaNet + DETERMINISTIC); coherence-gate PASS + serve-multiturn PASS on single-GPU. REMAINING (optional robustness per the VALIDATION MATRIX): coherence + serve-multiturn ALSO under HIPFIRE_EMULATE_GPUS=2 (the reset pp>1-branch concern — parity emu2 already exercised ar_generate+reset token-identically, so this is incremental).

## INC 2..N — ABSORB ALL ARCHES into ar_generate/ArchDispatch (bjoern 2026-07-10 "sequentially absorb all"; plan via fork a1a29ab6)
Dispatch map (fn generate @8018): qwen35(5/6) DONE; llama(0/1) = embedded else-arm (shares preamble); the rest are SEPARATE fns run before the shared preamble — generate_qwen2@12505 (arch7@~8264), generate_lfm2moe@11146, generate_minimax@11449, generate_cohere2moe@11842, generate_deepseek4@10484(+_spec), VL generate_vl@12678/generate_vl_dots_ocr@13288.
KEY IMPEDANCE — forward/sample split: qwen35/qwen2/minimax/moe = SPLIT (forward writes logits, driver samples) → fit ar_generate. llama = FUSED (`llama::forward_scratch(token,rng)->(tok,rng)` samples internally) → needs the ONE shared extension.
SHARED EXTENSIONS (add once): (1) fused-decode hook `decode_step_sample(&mut self,gpu,token,seq_pos,cfg,ngram_scope,mask,rng)->Result<u32>` (default Err) + ar_generate picks fused-vs-split via features()/Option — needed by LLAMA, add in Inc 3. (2) separate generate_<arch> fns keep own preamble + call ar_generate with their outputs (pflash_*=None non-qwen35) — NO trait change. (3) abort_zero_recurrent/take_prefill_checkpoint = per-arch bodies (DeltaNet qwen35 / conv-state lfm2moe), no new hook.
GRAMMAR: only deepseek4 has a Matcher (init_grammar+ds4 newtype); qwen2/minimax/lfm2moe/cohere2moe = init_grammar default None.
SEQUENCE + effort: Inc2 qwen2(S-M, greedy/no-grammar/no-recurrent/split; port ENABLES temp>0; parity@temp0=argmax — watch argmax tie-break vs gpu.argmax_f32) → Inc3 llama(M, embedded, adds fused hook) → Inc4 minimax + Inc5 cohere2moe(M, MoE-attn pair) → Inc6 lfm2moe(M-L, +conv-state recurrent hooks) → Inc7 deepseek4(L, DSML grammar+EP+MTP). DEFER: VL(image embeds) + spec-decode ("fold spec" follow-up #2).
EACH INC: implement <Arch>Dispatch + route (dead-code/parity-flag) → dual-run parity (temp0, state-appropriate) → coherence-gate → flip+delete old loop → commit. Same discipline as qwen35 (1.4a-d).
- Inc 2 (qwen2): BUILD-ONLY DONE (commit 27bfadf3, daemon.rs only — NO arch_dispatch.rs change, so ar_generate was already generic enough; validates the "separate-fn via own preamble" pattern). Qwen2Dispatch = greedy split (decode_step_forward→qwen2::forward_step writes logits; sample→gpu.argmax_f32 == fused forward_step_greedy byte-for-byte); eos_token→cfg.eos_token_id; reset→Qwen2State::reset; 5 accessors (tokenizer/seq_pos/set_seq_pos/conversation_tokens_mut/vocab_size); tangle hooks + init_grammar all default no-op/None (qwen2 has none); ensure_decoded_vocab NOT needed (grammar-gated, qwen2 grammar=None). Dual-run parity wired in generate_qwen2: legacy loop captures committed tokens (at the forward_step_greedy commit site) → inline Qwen2State reset → re-run ar_generate with greedy/no-frame params (temp0/max_think0/budget0/tools None/im_end_token None→no trailer/raw-prompt new_tokens) → assert_token_parity; flag off = legacy loop only. Impedances resolved: forward_step_greedy=forward_step+argmax_f32 (split==fused); max_think0/budget0 disable those paths; state.next_pos internal (seq_pos advisory); raw prompt (no framing). WATCH (for controller GPU gate): legacy stops on cfg.eos_token_ids SET vs ar_generate eos_token()==[0] + tokenizer.is_terminator(eos_id|eot_id) — confirm they coincide on real output. NEXT: controller GPU-validate parity (HIPFIRE_ARCHDISPATCH_PARITY=1 + HIPFIRE_DETERMINISTIC=1 on qwen25-0.5b-instruct.mq4, greedy) → flip generate_qwen2 body to ar_generate call + delete legacy loop → coherence.

## BACKLOG / FUTURE INCREMENTS (bjoern 2026-07-10 — recorded, not yet planned)
Detail + rationale in agent-memory note [[daemon-god-struct-archdispatch-design]] "FOLLOW-UPS" section.
1. Grammar hook cross-arch: `init_grammar`/`GrammarMatcher` is qwen35-only; wire deepseek4 (`deepseek4::grammar::Matcher`) + qwen2 tool-call matchers through the same hook (per-arch newtype) when they move onto ar_generate.
2. ArchDispatch beyond AR — fold in speculation: `as_spec_target()` stubbed None (qwen35 SpecTarget is guard-based, doesn't fit the sig). Reshape the spec bridge to yield the guard OR route the spec loop through ArchDispatch → one generate driver for AR + spec.
3. Port remaining arches to Steps dispatch (SEPARATE thread from ArchDispatch): minimax/lfm2moe/cohere2moe/dots-ocr/ds4-EP/qwen35-MoE+DeltaNet off bare execute_steps/hand-blocks → decompose MoE/DeltaNet into Steps. Orthogonal + composable with ar_generate. See moe-deltanet-decompose-into-steps + device-mesh-pivot-execute-steps-spine notes.
Inc 2..N (this plan) already sequences the AR-path arch ports (qwen2→llama→minimax/cohere2/lfm2moe→deepseek4→qwen35-MTP→VL); items 1-2 attach to those; item 3 is the device-mesh Step thread.

## GOTCHA — implementer self-amends AFTER its DONE report (Tasks 0.1 & 1.1)
- The general-purpose implementer sometimes keeps running past its first DONE message, does more self-review, and amends/adds a commit. => the SHA I snapshot for review can go stale. MITIGATION for next dispatches: tell the implementer to finish ALL self-review + fixes BEFORE the final report and NOT continue after; and re-check `git rev-parse HEAD` right before generating the review package.

## GOTCHA (applies to every lib.rs-touching task)
- `scripts/fmt-changed.sh` ALPHABETICALLY re-sorts the previously-unsorted `pub mod` block in lib.rs -> pulls unrelated reordering into your diff. After running it, `git diff` and revert any `pub mod` reordering that isn't your added line. (Hit in Task 0.1; implementer amended 31382a69->bdf9d678.)

## CRATE-PLACEMENT RULE (corrects the plan's file paths)
- State-OWNING structs (SessionState, and the owning ModelParallel enum) live in **hipfire-loader** (next to LoadedModel — the only crate that sees AsstTurnCache + DeltaNetSnapshot + EpState + hipfire_runtime::{TpModel,Gpus}). Concrete, no generics.
- BEHAVIOR traits (ArchDispatch) live in **hipfire-runtime** (loader depends on runtime → can impl/consume). Task 0.1 correct as-is.
- => Task 0.3 ModelParallel goes in hipfire-loader, not runtime. Update the 0.3 brief/dispatch accordingly.

## Inc N+1 design notes (surfaced during Inc 0)
- Dispatch is NOT strictly axis-first: `pp>1` (PpQwen35) is checked AFTER the arch-id short-circuits (7/9/11/12/10) in generate(). So an arch-specific model with pp>1 hits arch dispatch before the PP branch. The ModelParallel `match` in Inc N+1 must preserve this interleaving, not assume clean axis-first. (Task 0.3 finding.)

## DESIGN FORKS for later Inc-1 tasks (surfaced in 1.2 + 1.3 scoping)
- **Task 1.4 spec-bridge**: qwen35 SpecTarget is GUARD-based (`Qwen35Carrier::spec_target_guard`→`Box<dyn SpecTargetGuard>` RAII, MOVES bundle out of m.state, reopens HfqFile, restores on Drop). The ArchDispatch trait's `as_spec_target()->Option<&mut dyn SpecTarget>` signature does NOT fit. 1.4 must either reshape the trait's spec bridge to yield the guard, or route ar_generate's spec arm through the existing Carrier::spec_target_guard (NOT via ArchDispatch). (1.2 returned None+TODO.)
- **Task 1.3 reset-collapse scope**: reset is NOT qwen35-only. Sites: reset-cmd(2406-2492) + VL-guard(2183) + blocks@2661/2833 do a FULL MULTI-ARCH union (qwen35+speculator+qwen2/ds4/lfm2moe/minimax/cohere2moe + kv_adaptive); qwen35-arm prefill-abort/decode-abort do a SUBSET (seq_pos+conv+free prefill_ckpt+qwen35 dn memset+kv.compact_offset, NO dflash-free/speculator). model_reset_context as the UNION is behavior-identical at the multi-arch sites (non-loaded arches' `if let Some(X_mut())` = no-op) BUT folding it into the SUBSET abort sites would ADD ops (free dflash_ckpt, speculator.reset) → possible behavior change. DECISION PENDING w/ bjoern.

## Minor findings roll-up (for final review)
(none yet)
- Inc 2 (qwen2) DONE: Qwen2Dispatch (27bfadf3, split forward/argmax, no new hooks — confirms ar_generate genericity) + is_eos arch-stop hook (e1ce5a8b, qwen2=eos_token_ids set / qwen35=eos||is_terminator, qwen35 byte-identical) + FLIP (this commit, −135L, generate_qwen2 = preamble→ar_generate). UPLIFT decision (bjoern): qwen2 gains loop guard; validate clean-prompt parity (HELD: 2+2/gravity/cats/explain) + coherence (NOT strict all-prompt parity — degenerate raw-prompt loops force-stop = intended). ROOT of the one parity divergence = ar_generate's n-gram loop guard (confirmed via HIPFIRE_NGRAM_LOOP_THRESHOLD=0 → held), NOT is_eos.
=== INC 2 COMPLETE + VALIDATED (2026-07-10). Coherence (prod, no flag): qwen2 qwen25-0.5b-instruct "Paris." correct-start (then same raw-prompt degeneration generate_qwen2 always had — pre-existing framing issue, NOT a regression); vibethinker no panic/error (known raw-prompt degenerate); qwen35-4b regression CLEAN (coherent "...capital of France is Paris" — is_eos byte-preserving). Commits 27bfadf3/e1ce5a8b/c2ccf933. FOLLOW-UP: qwen2 feeds RAW prompt (no ChatFrame) → degeneration-prone; now on ar_generate, routing through proper framing would fix it ([[vibethinker-framing-rootcause]]) — separate improvement, out of absorption scope. ===
- Inc 3 (llama, arch 0/1): IN PROGRESS. Embedded else-arm in generate() (shares preamble like qwen35). Needs the ONE shared extension: fused-decode hook (llama::forward_scratch samples internally → doesn't fit the split forward+sample). llama arm already has loop guard/is_terminator (isomorphic to qwen35) → STRICT parity provable.
- Inc 3 (llama) FLIP DONE (path-a uplift): llama else-arm (was 10150-10312) → LlamaDispatch{m} + ar_generate(.., pflash None×3, tape None); −163L legacy fused forward_scratch arm gone (grep 0 in generate range). Build clean (daemon+lib). LlamaDispatch is_eos present (from 0baca0b9). NOT byte-identical (batched forward + generic sampler vs legacy fused forward_scratch w/ kernel-baked penalty) → controller coherence-gates + measures tok/s + regression qwen35/qwen2.
=== INC 3 COMPLETE + VALIDATED (2026-07-10, commits 0baca0b9 LlamaDispatch + 26be61d1 flip). COHERENCE: qwen3-0.6b-llama + qwen3-8b both fluent (<think> reasoning), no panic. PERF A/B (warm, vs legacy@0baca0b9): 0.6b 256.0→258.5 (+1%), 8b 18.0→18.4 (+2%) — B=1 batched-decode is PERF-NEUTRAL (within ±1-3% noise), fork's perf concern FALSIFIED. Path-a uplift is a clean win (all features gained, zero perf cost). Regression qwen35-4b 57 tok/s coherent + qwen2 228 coherent. llama uplifted; strict parity intentionally N/A (coherence+perf is the oracle per bjoern). ===
- Inc 4 (minimax, arch 10): MinimaxDispatch GROUNDWORK DONE (commit 66247ae5, dead-code, build clean daemon+lib). generate_minimax@11680-12072. VERIFIED: model_reset_context resets MiniMaxState (parity-sound); MiniMaxState.logits is GPU-resident (forward writes it + downloads Vec) → STATELESS dispatcher (sample re-downloads state.logits, double-D2H free on UMA). MinimaxDispatch (mirror Qwen2Dispatch): prefill_forward=forward_batch sub-chunked-64 (or decode_step)/decode_step_forward=decode_step/sample=download state.logits+sample_cpu (UPLIFT; temp0=argmax parity, temp>0 differs)/eos+is_eos=b.eos_tok/seq_pos→state.n_tokens/set_seq_pos→state.n_tokens/vocab=config.vocab_size; tangle+grammar+asst-cache = trait defaults (arch10 has none). NOT WIRED — the dual-run in generate_minimax is intricate: (a) KEEP preamble (Jinja frame / interleaved-thinking LCP PARTIAL-reuse → prefill_ids / capacity-guard state.reset / primed_think), (b) emit primed_think "<think>\n" in preamble BEFORE ar_generate (display-only, not in tape → parity-neutral), (c) dual-run = legacy prefill+decode captures __old_tape at emit-site 12020, then model_reset_context + MinimaxDispatch + ar_generate(temp0, tools None, pflash None, primed_think already emitted, framing via new_tokens=prefill_ids, tape Some) + assert. RISKS for controller GPU gate: (1) sample_cpu temp0 argmax tie-break vs legacy deepseek4::sampling::sample_token argmax — must coincide for parity; (2) position/state.n_tokens sync (decode_step advances n_tokens internally; driver passes seq_pos as position — seeded from seq_pos()=state.n_tokens). MODEL: MiniMax on box = ornith-35b-aeon.mq6 (35b, SLOW ~28 tok/s) — parity validation is heavy. NEXT: controller wires dual-run + GPU-validates temp0 parity + coherence + flip.
=== INC 4 COMPLETE + VALIDATED (2026-07-10; ab6e29fe dual-run wiring → 638e7353 flip → 42d64251 eos-marker fix). VALIDATION MODEL = MiniMax-M2.7.mq2 (arch 10, 79GB) — NOT ornith-35b-aeon.mq6 (that's qwen3_5_moe = arch 5/6 VL; the groundwork note's pointer was WRONG). Flow per pattern: (1) dual-run wiring build-only (legacy loop captures __old_tape incl terminal-eos, model_reset_context + ar_generate→__new_tape + assert_token_parity); (2) TEMP0 PARITY PROVEN — 3 prompts cap/code/reason, full 40/48/48-tok greedy, assert never fired, decode 27.9 vs 27.87 tok/s (no regression); (3) FLIP — generate_minimax = Jinja/LCP-partial preamble + primed_think re-emit → ar_generate(new_tokens=prefill_ids from seq_pos=state.n_tokens, cached=prompt−prefill, tape None); −140L legacy. Prod coherence (no flag): 4 turns fluent, MULTI-TURN LCP partial-reuse CONFIRMED under ar_generate (t2 lcp=49 cached=49 partial=true; is_eos→finish_reason=stop). REGRESSION CAUGHT+FIXED (temp0 parity structurally missed it — prompts hit max_tokens, never eos): minimax eos token IS `[e~[` decoding to a LITERAL (vs ChatML `<|im_end|>`→empty); ar_generate commits+emits eos before is_eos break → leaked. Fix = ArchDispatch::eos_filter_config() hook (default empty pass-through = qwen/llama byte-identical; MinimaxDispatch stop_at `[e~[`); re-validated eos-terminated turn ends `...Berlin.` clean, 0 markers, finish_reason=stop, cached=49. tie-break risk (sample_cpu vs sample_token argmax) — coincided (parity held). Next: Inc 5 cohere2moe (arch 12) — MUST override eos_filter_config (its `<|MARKER|>` state machine; coherence-gate-cohere2moe.sh hard-fails on marker leak) + validate an eos-terminated turn. ===
=== INC 5 COMPLETE + VALIDATED (2026-07-10; f5133030 Lfm2MoeDispatch+dual-run → 529b8c3a flip). RE-ORDERED (bjoern): lfm2moe done BEFORE cohere2moe (which needs a StreamParser hook — marker state machine + tool_calls parsing, cohorted with ds4). lfm2moe = plain per-token AR loop; feared conv-state hooks stay NO-OP (cold-reset-every-turn recovers post-abort state). Arch delta = stop-token FAMILY: is_eos over a stop_ids SET (special_token_id-resolved <|im_end|>=eos_tok + <|endoftext|> + </s>, the latter two don't round-trip through encode) + eos_filter_config stripping all 3 literals (ar_generate commits+emits before is_eos break). VALIDATED lfm2.5-8b-a1b.mq4: temp0 parity HELD (cap/code, tapes identical incl terminal eos — legacy 79 emitted vs ar_generate 80 committed = eos-commit delta, assert never fired); ar_generate arm 0 marker leaks (<|im_end|>/<|endoftext|>/</s>); decode 158 vs 159 tok/s no regression; coherent well-formed <think> blocks. REMAINING AR arches = the StreamParser COHORT (cohere2moe arch 12 + deepseek4 arch 9): build ArchDispatch::stream_parser()->Option<Box<dyn StreamParser>> once, brainstorm shape first. VL (dots-ocr/qwen35-vl) + all spec-decode still deferred. ===

## Axis A (StreamParser output-unification) — plan 2026-07-10-streamparser-output-unification.md
- Task 1: complete (commit f9327987..<head>, StreamParser trait + StreamAction/EosDecision in runtime lib stream_parser.rs; done by controller after haiku implementer HALLUCINATED a bogus dspark report — verified repo state, wrote the transcription directly, build clean). Cheap-model lesson: verify implementer diffs, don't trust reports.
- Task 2: complete (commit d930b801, DefaultStreamParser + 6 unit tests, lib 342 passed, daemon build clean; fork w/ full context). Faithful port of ar_generate 8226-8431 (EosFilter emit / stop-seqs / think-cap counters+splice-via-next_forced / n-gram / in-think budget-alert). DRIVER-SIDE for T3: (1) on_eos=CommitAndStop → driver emits terminal eos WITHOUT feed's guards; (2) budget-alert !in_think RESAMPLE stays driver-side (TODO marked); (3) note_force_answer(check_force_answer(id)) each iter. Trait feed(tok,bytes) UNCHANGED (parser self-accumulates streamed/decoded). T4 temp0 parity is the real byte-identity gate. Controller reviewed diff (faithful).
- Task 6: complete (commit 5002a67f, 4 cohere2moe guard-forcing fixtures + README, md5s recorded). LIVE-TUNING CONCERNS for T8: (1) empty_turn fixture may answer immediately at greedy temp0 → may need subtler binary Q; (2) think_budget needs harness to set max_think_tokens (~100); (3+4) toolcall fixtures need a runtime tool schema supplied by the harness; guard4(text-recovery) vs guard3 separation fragile → maybe temp0.3-0.5 for guard4. Comments in README (gate cats prompt file verbatim → no #-lines in prompt).
- Task 3: complete (commit d5a5bbd3, daemon+lib clean, 342 tests). Routed ar_generate through DefaultStreamParser: stream_parser() hook + 3 trait methods (emit_only/note_force_answer/force_answer_latched). on_eos→emit_only(terminal eos, no guards); feed(sampled); emit_only(forced think-cap splice via was_forced); budget-alert kept DRIVER-SIDE verbatim (parser budget_alert_at_tok=0). Inject arm = bare break (Default never returns it; cohere2moe T7 needs eos-before-commit). T4 RISKS: think-cap splice next_forced-drain ordering (HIGHEST), budget-alert path, marker-eos emit_only strip.
- Task 4: IN PROGRESS (controller GPU). NOTE: 5 arches already on ar_generate (no legacy loop) → T4 is CROSS-COMMIT byte-identity (f9327987 pre-refactor vs d5a5bbd3 post), NOT dual-run parity.
- Task 4: complete + PASSED (controller GPU, cross-commit byte-identity f9327987 PRE vs d5a5bbd3 POST). qwen35-4b plain/code/reasoning BYTE-IDENTICAL (committed tok_ids + visible text, cmp.py). lfm2moe cap(eos-terminated stop)/sheep BYTE-IDENTICAL, emit_only marker-strip preserved (0 <|endoftext|>/<|im_end|> leaks). qwen35 asst-turn-cache fingerprint INTACT (m2b exact-history → cached_tokens=63 hit, fp=0xe126b18091a4d645). RESIDUALS (low-risk): think-cap force-close NOT force-fired (4B models reason in plain text, no <think> tags → currently_in_think=false; faithful reviewed port); budget-alert kept driver-side verbatim (T3); minimax [e~[ = same emit_only mech as lfm2moe (covered); qwen2/llama = subset of shared DefaultStreamParser path (covered). Byte-identity is the strongest oracle — refactor confirmed behavior-preserving.
- Task 5: complete (commit 61c46e5f, Cohere2MoeStreamParser in daemon.rs + 2 section-machine unit tests, daemon clean, lib 342). Sec machine + forced queue + on_eos empty-turn Inject + tool_calls(parse_cohere_action/snap_call_names) + finish vis_buf recovery. Added trait `enqueue(&mut self, tok)` (default no-op; cohere pushes to forced queue). T7 MUST: wire driver on_eos→Inject(v) → for t in v { parser.enqueue(t) } then continue WITHOUT committing the eos (T3 left Inject arm a bare break — the eos-before-commit structural change). ToolCalls(v) → driver render at daemon.rs:8236 wraps as {"type":"tool_calls"} (confirmed). cohere2moe FAITHFUL not byte-identical → T8 event-equivalence is the bar.
- Task 7: complete (commit 36a49b4d, daemon clean, lib 342). Cohere2MoeDispatch (full ArchDispatch, minimax-modeled: forward_batch/decode_step, sample_cpu, is_eos=END_OF_TURN eos_tok, stream_parser()→Cohere2MoeStreamParser::new(tok,tools,max_tokens,max_think)). DRIVER eos-check MOVED PRE-COMMIT (loop top) to support Inject: Inject→enqueue markers+next_forced no eos commit; CommitAndStop→commit+forward+emit_only+break (fork claims byte-identical same-order). Dual-run wired in generate_cohere2moe (legacy intact, T9 deletes). T8 MUST: (a) RE-CONFIRM 5-arch byte-identity (eos path changed in shared driver); (b) cohere2moe token-parity floor + coherence-gate-cohere2moe.sh + 4 guard fixtures (event-equivalence = the real bar).
- Task 8 (partial: token-parity FLOOR): PASSED after debug-fix. Dual-run caught a REAL bug: model_reset_context had NO cohere2moe reset (reset qwen2/ds4/lfm2moe/minimax but MISSED cohere2moe) → ar_generate arm ran on legacy arm's stale n_tokens+KV → argmax flip at tok19 (len 55 vs 67). FIX cf754c74: added m.cohere2moe_mut().state.reset(gpu) to model_reset_context (also closes a LATENT #462 cohere2moe reset-cmd bleed) + Cohere2MoeDispatch.prefill_forward chunk 64→256 (matches legacy i+256; moot for mq4=per-token but correct for batched tiers). Re-run: PARITY HELD cap/code/sheep (exit 0, no PARITY FAIL). NOTE: reset fix is cohere2moe-only → 5-arch byte-identity unaffected. REMAINING T8: event-equivalence (coherence-gate-cohere2moe.sh + guard fixtures) — do POST-flip on the ar_generate prod path (better than legacy baseline).
- Task 9: complete (commit 0a635eb1, flip cohere2moe → ar_generate, −467 lines legacy loop deleted, daemon+lib clean, 342 tests). Cohere2MoeStreamParser owns Sec machine/forced/guards/tool_calls/vis_buf-recovery. LCP preserved (new_tokens=prefill_ids, seq_pos=state.n_tokens=lcp). Minor: primed_think now unused (cohere2moe uses markers not <think> primer) → underscore in T10. Event-equivalence GPU validation (coherence-gate-cohere2moe.sh + guard fixtures) on flipped prod path IN PROGRESS.
- Task 8: COMPLETE + PASSED (event-equivalence on flipped ar_generate prod path). coherence-gate-cohere2moe.sh: cap/reason/code/long-context all OK, 0 marker leaks/errors (long-context = sliding-window flash >4096 covered). Guard fixtures: TOOL-CALL emits valid {"type":"tool_calls"} [{"name":"get_weather","arguments":{"city":"Tokyo"}}] (the path token-parity is blind to — WORKS); empty-turn coherent answer; think-budget coherent (force-close didn't fire = fixture-tuning/model-behavior like qwen35-4b, not a bug); 0 marker leaks anywhere. cohere2moe migration FULLY VALIDATED.
- Task 10: complete — docs updated (design note: Axis A DONE + Axis B=ModelParallel-collapse-brings-ds4-EP; eos-checklist: cohere2moe done via StreamParser marker machine + the model_reset_context-must-reset-migrated-arch lesson + batch-size-numeric-sensitivity). primed_think unused-warning cleanup attempted then reverted (global sed over-matched minimax/lfm2moe; harmless warning left for final review). daemon.rs clean vs 0a635eb1.
=== AXIS A COMPLETE (2026-07-10). StreamParser output-unification: trait+DefaultStreamParser (5 arches byte-identical, GPU-proven) + Cohere2MoeStreamParser/Dispatch (cohere2moe migrated, −467L, token-parity floor + coherence-gate + tool_calls validated). Dual-run caught+fixed a real missing-reset bug (+latent #462). Commits 2fc16d7b..0a635eb1 (+ reset fix cf754c74 folded in). Axis B (ds4-EP via ModelParallel collapse) = next increment. ===
- FINAL REVIEW (opus, whole-branch f9327987..f7384baf): sound; 1 Critical (ar_generate missed parser.finish() → cohere2moe Guard-4 recovery dead) FIXED (commit below). Minors non-blocking: primed_think unused-warning; DefaultStreamParser in-think budget branch dead (config budget_alert_at_tok=0, driver keeps budget-alert); cohere2moe asst-cache uses <think>-strip fingerprint (markers not stripped — benign); pad/repeat-guard commits 1 extra garbage tok before stop (immaterial). Verified correct: eos disciplines, think-cap splice, byte-delta contract, marker machine (no leak/double-inject), model_reset_context fix, spec paths untouched.
=== AXIS A COMPLETE + REVIEWED + MERGE-READY. Final Critical fixed. Commits 2fc16d7b..HEAD. ===

## AXIS B (deepseek4-EP onto ar_generate) — SPEC + PLAN + HANDOVER written (NOT started, 2026-07-10)
Spec: docs/superpowers/specs/2026-07-10-axis-b-modelparallel-collapse-design.md (EP-only, FNV oracle, decisions adopted). Plan: docs/superpowers/plans/2026-07-10-axis-b-modelparallel-collapse-plan.md (6 increments). HANDOVER: docs/superpowers/plans/2026-07-10-axis-b-handover.md (fresh-session resume). Crux = inc1 device-access inversion (ForwardCtx marker-ctx vs device-in-bundle — brainstorm first). EP AR is MTP-free clean subset; grammar 1:1; dsml needs a wrapper; parity = DS4_EP2_FNV anchor + dual-run emulated-EP-2. Execution deferred to a NEW session per bjoern.

## AXIS B EXECUTION STARTED (2026-07-10, new session)
- Brainstorm DONE: inc-1 device-access fork resolved. bjoern chose **option A** — `ForwardCtx { Single(&mut Gpu), Mesh }` unit marker. Spec's `Mesh(&mut Gpus)` PROVEN borrow-illegal (Explore-mapped: ar_generate takes dispatch=&mut m + device separately; EP's only Gpus lives at m.ep.gpus = overlapping &mut m borrow). EP dispatch reaches m.ep.gpus via &mut self. Spec doc updated (gitignored-local) + decision #4 recorded.
- FORCED detail (pre-flight): `sample` receiver `&self`→`&mut self` (EP arm reaches m.ep.gpus through self; Mesh marker carries no device). Mechanical superset for the 6 single-GPU impls.
- Base commit for Inc 1 = 508f670b.

=== AXIS B INC 1 COMPLETE + VALIDATED (2026-07-10, commit 7652570f, base 508f670b) ===
ForwardCtx { Single(&mut Gpu), Mesh } threaded through the 7 device hooks; sample &self->&mut self; 22 impl bodies wrapped byte-identical via `let ForwardCtx::Single(gpu)=ctx else {unreachable!}`; 19 ar_generate call sites pass Single(&mut *gpu). reset+free_prefill_checkpoints intentionally left on gpu (not inc-1 scope; inc4 revisits at the ar_generate signature). Build: workspace+daemon(deltanet) clean, 342 lib tests. INDEPENDENT REVIEW (opus): Spec ✅ / Quality Approved, 0 Critical/Important (2 informational Minors: dead-until-inc4 Mesh arm + let-else — both deliberate). GPU BYTE-IDENTITY (cross-commit 508f670b-old-binary vs 7652570f-new-binary, EMIT_TOKEN_IDS+DETERMINISTIC, temp0): qwen35-4b.mq4 plain(48/48 md5 b210d894663c) + code-LRU(120/120 md5 a845a598e101) BYTE-IDENTICAL; lfm2moe.mq4(64/64 md5 3bc06193c74d) BYTE-IDENTICAL. qwen35=all-7-hooks GPU-sampler; lfm2moe=host sample_cpu path → both wrap shapes proven no-op. Byte-identity subsumes coherence-gate (cohere2moe StreamParser is output-side, untouched by inc1). Base for Inc 2 = 7652570f.

=== AXIS B INC 2 COMPLETE + REVIEWED (2026-07-10, commit a82b6b3f) ===
Deepseek4EpDispatch — first mesh ArchDispatch (build-only, not wired). Mirrors ep_serve_ds4: per-token forward_ep prefill(loop chunk)/decode, per-rank reset(bind_thread/reset/zero_decode_caches/invalidate_graph_state), rank-0 logits download + HOST sample_full_dist. ForwardCtx::Mesh arm reaches m.ep.gpus via &mut self; Single=>unreachable. Tangle hooks trait-default. Build clean, 342 tests. INDEPENDENT REVIEW (opus): ✅ FAITHFUL, 0 Critical/Important; 3 forward-looking Minors for inc4: (1) is_eos primary-eos-only faithful but confirm ar_generate generic im_end/stop path adds no eos break ep_serve lacked; (2) prefill positions must stay seq_pos+i contiguous if driver sub-chunks; (3) vocab/seq_pos fallback arms cosmetic.

=== AXIS B INC 3 COMPLETE (2026-07-10, commit 0a4dd7f3) ===
deepseek4 DSML output: Deepseek4GrammarMatcher newtype + Deepseek4StreamParser (wraps dsml, Option for self-consuming finish; feed=utf8-lossy(bytes)->inner.feed->map_events; Token→visible Emit/Reasoning→reasoning Emit/ToolCalls→{name,arguments} array). Wired via init_grammar (rebuilds FULL ToolSchema incl params from stored self.tools — trait tuple drops params) + stream_parser (self.think_mode). +2 dispatch fields (tools, think_mode). 4 unit tests pass (map_events). 2 DELIBERATE plan deviations, both faithful to ep_serve_ds4: (a) on_eos=Stop not CommitAndStop (ep_serve breaks pre-feed, eos never enters KV/tape/parser; driver handles Stop@8763); (b) init_grammar from self.tools not trait tuple (params load-bearing). Build clean, 342 lib tests. Base for inc4 = 0a4dd7f3.

=== AXIS B INC 4 COMPLETE (2026-07-10, 4a=39884d22, 4b=2b6f3cf1) ===
4a: ar_generate takes ctx:ForwardCtx (from gpu:&mut Gpu) + ForwardCtx::reborrow() + free_prefill_checkpoints→ctx; 6 callers wrap Single(gpu); behavior-preserving single-GPU (342 tests). 4b: EP gate → ar_generate, build-only dual-run behind HIPFIRE_ARCHDISPATCH_PARITY=1. ep_serve_ds4 +optional TokenTape; ep_reset_ds4_state helper (per-rank, uses m.ep.gpus); ep_serve_ds4_via_ar_generate (Deepseek4EpDispatch{m,tools,think_mode}+Mesh+30-arg ar_generate call, cold-prefill full DSML prompt, im_end/think_pair empty=dsml owns). generate_ep arch-9: parity→dual-run(assert_token_parity old vs new)/else legacy. Build clean, 342 tests. **CRITICAL for inc5 flip: model_reset_context does NOT reset m.ep (Axis-A cohere-reset-bug class) — flip must guarantee per-turn EP reset (recommend generate_ep calls ep_reset_ds4_state before ar_generate + add EP block to model_reset_context for the reset command).** Inc 5 = GPU (FNV DS4_EP2_FNV + dual-run parity emulated-EP-2 + events/coherence) then FLIP.

=== AXIS B INC 5 — GPU DUAL-RUN VALIDATION PASSED (2026-07-10) ===
FNV anchor (ep_deepseek4 example): PRE-EXISTING baseline drift, NOT my regression — deepseek4 crate is BYTE-UNTOUCHED (diff = daemon.rs+arch_dispatch.rs only); pinned 0x6c0f2f000f1d398f vs box output 0x26a13602bedf9926 is environmental (pflash-stage class). FNV unusable here; dual-run is the correct oracle. DUAL-RUN PARITY (daemon, HIPFIRE_EMULATE_GPUS=2 + DETERMINISTIC + ARCHDISPATCH_PARITY, ep:2, greedy): ep_serve_ds4 vs ar_generate+EP committed-token parity HELD on capital/reasoning/code/tool-call. Coherent both arms (Paris; step-by-step speed; fibonacci; tool call). Caught+fixed a REAL bug: Deepseek4EpDispatch missing ensure_decoded_vocab → grammar path panicked at arch_dispatch.rs:323 unimplemented!() (fix commit below). After fix: tool-call PARITY_FAIL=0, both arms emit identical {get_weather,Tokyo}. NEXT: FLIP (route prod through ar_generate + per-turn ep_reset_ds4_state; delete ep_serve_ds4 + dual-run scaffold), re-validate on flipped path, then inc 6 docs.

=== AXIS B INC 5 COMPLETE + FLIPPED + VALIDATED (2026-07-10, c4ab43c2 fix + flip commit) ===
FLIP: prod ds4-EP → ar_generate; ep_serve_ds4 DELETED (−420L); generate_ep arch-9 = per-turn ep_reset_ds4_state + ep_serve_ds4_via_ar_generate. Shared ep helpers stay (minimax-EP). POST-FLIP VALIDATION (emulated EP-2, deterministic, NO parity flag): capital "**Paris**"/reasoning step-by-step coherent, tool-call → single correct {get_weather,Tokyo} event, 0 panics, SINGLE arm (no doubling = ep_serve_ds4 gone). MULTI-TURN #462: t1 Tokyo / t2 Berlin (no bleed), 0 errors — per-turn reset works. model_reset_context EP gap is BENIGN (generate_ep resets per-turn before every generation → EP always fresh at gen; no serving path reads EP state without generate_ep resetting first). Build clean, 342 tests. Axis B EP-fold DONE. Remaining: inc 6 docs + final whole-branch review.

=== AXIS B FINAL WHOLE-BRANCH REVIEW + FIXES DONE (2026-07-10, commit e6e1629f) ===
Independent opus whole-branch review (508f670b..cef58c7b): ForwardCtx/dispatch/on_eos/reset all HOLD; found 4 regressions the validation matrix missed (oversized prompt / custom stop-seq / tool-call cache echo-back — none exercised by capital/reasoning/code/short-tool-call). FIXED + GPU-VALIDATED (emulated EP-2):
- C1 (Critical crash): re-added ep_serve_ds4's capacity guard to ep_serve_ds4_via_ar_generate (ar_generate has no pre-prefill cap check on EP). Validated: oversized prompt → clean {"type":"error"...capacity}, crash=0.
- I1 (stop-seqs): Deepseek4StreamParser honors cfg.stop_seqs (accumulate pieces, append Stop on suffix) + unit test. Validated: stop=["Paris"] truncates "...**Paris" (vs full "**Paris**.").
- I2 (finish_reason+fingerprint): driver captures parser-emitted ToolCalls (StreamAction) into parser_tool_calls → drives finish_reason+fingerprint when ChatML extractor is empty; wire re-emit guarded to text-recovered only (no double-emit). Fixes ds4 DSML AND cohere2moe markers. Validated: tool-call → finish_reason=tool_calls, single correct {get_weather,Tokyo} event.
Accepted minors (documented): M1 EP-abort set_seq_pos(0) no-op (self-healed per-turn reset); M2 model_reset_context EP gap (benign); M3 HIPFIRE_QWEN35_GRAMMAR now driver-wide grammar switch (historical name, rename=bjoern's call); M4 dead dual-run leftovers (#[allow]-masked). NOTE: I2 changes cohere2moe finish_reason(stop→tool_calls)+fingerprint on tool turns — strictly more correct, wire events unchanged, gate-orthogonal (coherence-gate-cohere2moe checks events/coherence/markers, not finish_reason) — a cohere gate re-run is advisable but not blocking. Build clean, 342 lib + 5 ds4 tests. AXIS B COMPLETE + REVIEWED + fixes validated. Ready for merge decision.

=== FOLLOW-ON STARTED: minimax-EP fold (bjoern chose it, 2026-07-10) ===
Reuse ForwardCtx/Mesh + the ds4 dual-run→flip pattern for ep_serve_minimax (arch 10). KEY DIFFS from ds4: (1) minimax-EP HAS LCP (prefix-cache partial reuse — rewinds rank KV to common prefix, prefills suffix only) → the ar_generate call needs real cached_tokens_count/new_tokens=suffix, NOT ds4's cold-prefill full-prompt; (2) output is non-DSML (ChatML-ish + primed_think interleaved-thinking, [e~[ eos marker) → MinimaxEpDispatch likely REUSES the existing single-GPU MinimaxDispatch output hooks (eos_filter_config/default stream_parser), differing only in forward/sample (multi-rank forward_ep + rank-0 host sample). Explore mapping ep_serve_minimax contract in flight. Validation model = MiniMax-M2.7.mq2 (arch 10, ~79GB) EP-2 emulated. Plan: MinimaxEpDispatch (build-only) → dual-run wiring → GPU parity → flip (delete ep_serve_minimax) → docs.

=== MINIMAX-EP FOLD COMPLETE (2026-07-10, M1 MinimaxEpDispatch + M2 dual-run + M3 flip) ===
Second mesh arch onto ar_generate. M1: MinimaxEpDispatch (forward_ep/sample over EpArch::Minimax; reuses MinimaxDispatch output — [e~[ eos filter, DEFAULT StreamParser which honors stop-seqs, no grammar; n_tokens usize). M2: ep_serve_minimax +tape; ep_serve_minimax_via_ar_generate (mirrors single-GPU generate_minimax LCP preamble + <think> primer, PER-RANK n_tokens rewind); dual-run behind parity flag (no reset between arms — LCP self-heals: full-prompt prior tape→cache miss→cold prefill). M3: FLIP + deleted ep_serve_minimax + 4 orphaned ep_emit_* helpers (~530L; only served the folded EP loops). VALIDATED (emulated EP-2): dual-run parity capital/code PASS; post-flip coherent single-arm + multi-turn t1 Tokyo/t2 Germany no-bleed, 0 panics. Passes real stop+top_k/min_p (better than single-GPU minimax flip which passed &[]). Both EP arches (ds4+minimax) now on ar_generate; ep_serve_* fully gone. Remaining EP-adjacent: dense TP/PP fold (deferred). Docs = M4.

=== FOLLOW-ON 2 STARTED: dense TP/PP fold (bjoern "fold dense", 2026-07-10) ===
Last multi-GPU driver onto ar_generate = the ModelParallel::Dense arm. generate_dense drives a DenseServed (TpModel|PpModel, holds Gpus in m.tp/m.pp_dense → ForwardCtx::Mesh). Spec says "Single↔Dense merge is easy — DenseServed(forward_token/logits/prefill/eos_token) is a clean per-token abstraction". Plan: DenseDispatch (mesh, wraps DenseServed via &mut self) → dual-run → GPU parity (pp-gate / tp) → flip (delete generate_dense) → docs. Explore mapping the contract.

=== DENSE TP/PP FOLD COMPLETE (2026-07-10, D1 DenseDispatch + D2 flip+delete generate_dense) ===
Third mesh dispatch onto ar_generate — the LAST separate multi-GPU driver. DenseDispatch wraps DenseServed (TpModel|PpModel via is_pp; Gpus in m.tp/m.pp_dense → ForwardCtx::Mesh). prefill_forward = batched model.prefill at pos0 / per-token suffix on LCP hit (prefill_max_batch=MAX → one chunk); host sample_cpu; is_eos=eos||is_terminator; cursor on m.seq_pos. dense_serve_via_ar_generate: plan_prompt_cache LCP preamble → conv=rendered[0..start_pos] (truncate hit/clear miss) → ar_generate extend(new_tokens)+push = rendered+generated. Both tp+pp_dense gates flipped; generate_dense + dead DenseServed::eos_token DELETED. VALIDATED (emulated, deterministic, temp0): CROSS-COMMIT text-identity pp=2 (md5 94315878f6c3) + tp=2 (a0e5a9233bf9) old(generate_dense)==new(ar_generate) → fold byte-preserving; both coherent err=0. NOTE: pp=1(single-GPU llama) ≠ pp=2(dense) is EXPECTED (different render/sample paths — parity examples validate forward math not daemon-serve equiv; my fold left that relationship unchanged). pp-gate.sh needs 2 physical GPUs (skips on emulate). ALL AR PATHS now on ar_generate: 6 single-GPU + 3 mesh (ds4-EP, minimax-EP, dense TP/PP). ep_serve_ds4/ep_serve_minimax/generate_dense all gone. ModelParallel collapse effectively DONE at the driver level. Docs = D4.

=== 3-AGENT TEAM REVIEW of minimax-EP + dense folds (2026-07-10, fixes @ba0fc7a6) ===
Team: #1 minimax-faithfulness, #2 dense-faithfulness, #3 cross-cutting-regression. minimax = FAITHFUL (0 Critical/Important). dense = 1 real Important FIXED + minors. ALL VERDICTS + fix:
- I-1 (Important, CRASH REGRESSION, FIXED @ba0fc7a6): dense dropped the capacity guard (ds4/minimax kept it). generate_dense caught prefill's overflow Err→clean error; ar_generate .unwrap()s forward hooks→PANIC on oversized prompt. Re-added prompt_n+max_tokens>physical_cap guard. GPU-validated: oversized tp=2 → clean error, panic=0.
- Cleanup: removed dead archdispatch_parity_enabled; fixed broken [`generate_dense`] rustdoc link + stale routing comment.
ACCEPTED MINORS (documented, not fixed): (a) committed-eos → dense conv=rendered+gen+eos (MATCHES single-GPU llama; multi-turn LCP miss for eos!=<|im_end|> models — correctness-neutral perf-only); (b) .unwrap() on GPU forward errors panics (ARCHITECTURAL — all ar_generate arches since Axis A, not dense-specific); (c) model_reset_context doesn't reset m.ep/m.tp/m.pp_dense (BENIGN — LCP/re-prefill self-heals per-turn, reviewers confirmed no #462); (d) MinimaxEpDispatch::reset lighter than Deepseek4EpDispatch::reset (dead in-path; old ep_serve_minimax used only s.reset()); (e) minimax abort set_seq_pos stale (self-heals next turn); (f) tape params always-None dead plumbing. CONFIRMED SOUND by team: I2 finish_reason inert for minimax/dense (DefaultStreamParser emits no ToolCalls); 3 mesh dispatches borrow-sound + unreachable!() genuinely unreachable; pp=1≠pp=2 is pre-existing (not a fold regression). GPU: dense eos-terminated turn 0 marker leaks (LlamaDispatch precedent). Build clean, 342 tests.

=== QUALITY PASS (post-3-agent-review) COMPLETE (2026-07-10) ===
FIXED: I-1 dense capacity guard (ba0fc7a6, crash regression, GPU-validated clean error); (c) model_reset_context explicit mesh reset + (d) minimax reset comment + (f) dead tape params (627ad8f4, GPU-validated reset→generate coherent no-bleed); dead code (archdispatch_parity_enabled, stale generate_dense refs); (b) ar_generate hook .unwrap()→hook_or_fail! graceful error across ALL 9 arches (this commit — happy path byte-unchanged, qwen35+dense coherent 342 tests). LEFT documented-accepted: (a) dense committed-eos LCP (matches single-GPU llama, correctness-neutral perf-only); (e) minimax abort set_seq_pos (consistent w/ minimax-family, self-heals). ModelParallel driver collapse + follow-ons (minimax-EP, dense) fully reviewed + hardened. Ready for merge.

=== EXTERNAL WHOLE-BRANCH REVIEW + FIXES (2026-07-10, commit 17fc1c4c) ===
External reviewer graded the branch "8/10 foundation, 4/10 integration — emulated proofs labeled as production." 6 findings, all code-verified against HEAD (c9d36d0e):
- **#1 Dense PP emulation-only (REAL, known follow-up):** pp_serve.rs loads whole weights on out_dev; module doc admits real-HW per-stage banding is a follow-up. No capacity benefit; cross-device pointer deref on real HW. NOT fixed (structural — the actual PP-capacity win).
- **#2 TP/PP unload leaked all VRAM (REAL, NEW — 3-agent review missed it) → FIXED:** GpuTensor/DeviceBuffer/GpuPool have no freeing Drop; Gpu::drop only re-binds. unload_model's drop(tp)/drop(pp) freed nothing (EP arm already frees explicitly for this reason). Added TpModel::free/PpModel::free (typed frees→drain_pool→drop Gpus); WeightStore::free_all pub(crate). Builds clean; NOT yet GPU-validated (real load/unload VRAM-reclaim cycle pending).
- **#3 Peer access enabled before allocations (REAL, NEW) → FIXED:** hipfire-hardware doc mandates enable_peer_all AFTER allocs (else hipMemcpyPeer silently writes nothing on real multi-GPU). TP/PP called it right after from_mesh. Moved after all allocs in tp_serve/pp_serve. Emulation-invisible (single device).
- **#4 Manifest replication wrong for EP (REAL, latent):** weight_manifest.rs:68 `_ => group_along(Tp)` → singleton on Ep-only mesh (a test asserts device-0-only). Live EP uses hand forward_ep, not the manifest, so latent. NOT fixed.
- **#5 ArchDispatch "dead end/unrouted" (STALE):** superseded — ar_generate is the live generic driver, all 9 arches flipped onto it (Axis A/B + folds above). Self-ref sidestepped: dispatch structs built transiently per-call, not stored in LoadedModel. Reviewer's cited lines were a think-cap loop (stale rev).
- **#6 Parity self-check proves only the comparator (partly stale) → FIXED (honesty):** matches ledger minor (f). Per-flip dual-run is deleted post-flip; in prod all ar_generate callers pass tape:None. Relabeled the harness comment truthfully + strengthened --self-check-parity to test BOTH comparator directions (accepts equal, rejects divergent).
Commit 17fc1c4c (5 files, +145/−25). Builds clean (loader+runtime+daemon); self-check passes. NOT committed to any remote yet (branch has never been pushed). OPEN after this: #2 GPU-validate; #1 + #4 structural (real-HW PP weight residency; EP manifest replication); god-struct → LoadedModel field-count still large (dispatch collapse done at the driver, not the struct).

=== #2 UNLOAD LEAK — GPU-VALIDATED (2026-07-10, commit 862c11ec) ===
New harness examples/mesh_unload_vram.rs measures free VRAM (hipMemGetInfo) across
4 load/unload cycles per axis. gfx1151 emulated 2-rank, qwen3-0.6b-llama.mq4, under gpu-lock:
- TP: ~805 MB loaded, ~789 MB reclaimed/cycle, after-unload drift 50 MB / 4 cycles → PASS.
- PP: 623 MB loaded, 623 MB reclaimed/cycle, drift 0 MB / 4 cycles → PASS.
Pre-fix drop(tp)/drop(pp) freed nothing → would leak full model/cycle (~2.4 GB drift). #2 CLOSED.

=== #2 RESIDUAL DRIFT ROOT-CAUSED + FIXED (2026-07-10) ===
TP's 50 MB/4-cycle drift (PP was 0) = leaked HIP streams. TpModel::load creates a
per-device stream (active_stream=Some); nothing destroyed it. Added stream_destroy
to TpModel::free. Re-measured: TP after-unload free byte-stable 116219 MB × 4 → drift
0 MB, == PP. PP left untouched (active_stream=None invariant, no stream to leak).

Task 1 (dense-pp-residency): complete (commits eafd8663..3ad96c26, review clean — distributed HFQ load entry point).
Task 2 (dense-pp-residency): complete (commit 3ad96c26..88ab73fd, review clean — LlamaWeights::free_gpu_multi; device indices match load placement). MINOR (for final review): free_gpu_multi doc says "free_tensor binds the owning device" — imprecise wording, no behavioral impact.
Task 3 (dense-pp-residency): complete (commit 88ab73fd..2a019375, review PASS/Approved; fixed the one Minor — stale "whole weights on output stage" doc in free() → "per-stage-distributed"). PpModel now distributed weights+KV, no forward change.

=== DENSE PP PER-STAGE RESIDENCY DONE ===
Task 4 (dense-pp-residency GPU validation): COMPLETE (2026-07-11, gfx1151 emulated-2, qwen3-0.6b-llama.mq4).
- Distributed weights (Layout::from_gpus per-stage HFQ load): validated via pp_full_model_parity + pp_prefill_parity.
- Distributed KV (new_gpu_q8_multi, pp×→1× per-stage): validated via pp_decode_parity (25 tokens, KV grows across positions).
- free_gpu_multi (weight+KV teardown): validated via mesh_unload_vram pp (no-leak, drift -1 MB / 4 cycles).
- Byte-identical: pp_full_model_parity max|Δ|=0.000e0 (argmax 33450); pp_prefill_parity max|Δ|=0.000e0 (96-tok); pp_decode_parity FNV 0a73e4975b94d4b7, first_div=None.
- VRAM footprint: ~585 MB loaded / cycle (distributed weights+KV); full reclaim per cycle.
- Review finding #1 (PP emulation-only): still open — no real 2-GPU HW to prove per-device capacity split; proven self-consistent (byte-identical) on emulated-2. Review finding #2 (unload leak): CLOSED (committed eafd8663 + stream leak fix). Report: .superpowers/sdd/task-4-report.md.
Task 4 (dense-pp-residency): complete (commit 2a019375..460e3800). GPU-VALIDATED: pp_full_model_parity max|Δ|=0.000e0, pp_prefill_parity max|Δ|=0.000e0, pp_decode_parity 25-tok stream FNV-identical (pos>0 KV), mesh_unload_vram pp PASS (~585MB/cycle, full reclaim). All 4 tasks done.
=== DENSE PP PER-STAGE RESIDENCY DONE (2026-07-10, eafd8663..460e3800) ===
Review #1 closed for PP: distributed weights (Layout::from_gpus + load_weights_hfq_distributed) + distributed KV (new_gpu_q8_multi, pp×→1×) + LlamaWeights::free_gpu_multi; NO forward-pass change. 4 subagent tasks, each reviewed. Adversarially pre-reviewed (correctness/borrow/teardown — all sound). Real-HW per-device capacity split still needs 2-GPU HW (documented).
FINAL WHOLE-BRANCH REVIEW (opus): MERGE-READY, 0 Critical/0 Important. Verified real-HW placement correctness (bind-before-alloc on correct &mut Gpu), load/free symmetric by device_for_layer, single-GPU path byte-preserved, pp==devices.len() free assumption guaranteed by load guards. Carried "binds" Minor RESOLVED (doc is accurate). Open Minors (non-blocking, polish): (1) spec Validation#2 KV-halving assertion not coded — validated instead via no-leak arm + 3 parity oracles + observed ~585MB footprint; (2) reject_qwen2_hfq error msg terser (dropped inspect_hfq hint), behavior-neutral.

## ═══ Inc 2 Step E (qwen35_mtp_head fold + mtp_weights_present eliminate) ═══
Plan: docs/superpowers/plans/2026-07-11-god-struct-collapse-inc2e-qwen35-mtp-head.md
Spec: docs/superpowers/specs/2026-07-11-god-struct-collapse-inc2e-qwen35-mtp-head-design.md
Branch base for Step E: 1e25bb03
Split: implementer does no-GPU (build --workspace --all-targets + lib tests + commit); CONTROLLER runs GPU gates in-session.
- Task 1: pending — fold + eliminate (one atomic commit)
- Task 2: pending — GPU validation (serve-multiturn + MTP probe)
- Task 1: COMPLETE (commit f440c7e4, was 248f1ab1 pre-amend; workspace build clean, 343 lib tests pass; task-review spec ✅ + quality Approved; 2 stale daemon.rs comments fixed + amended; ⚠ skeleton_pp resolved — never named the fields). Minors (non-blocking): head freed first in unload arm (order-neutral); one blank line in load handler.
- Task 2: COMPLETE — GPU gates green. serve-multiturn PASS (AR qwen3.5-0.8b + DFlash qwen3.6-27b w/ .mtp sidecar → head round-trip exercised, no bleed). MTP-path probe HIPFIRE_QWEN_MTP=1 on qwen3.5-4b.mq4 (.mtp head loaded) verdict OK 0hard/0soft, 82 tok/s coherent.
- Final opus whole-branch review: READY-TO-LAND (no Critical/Important). Verified head lifecycle complete (no leak/loss across from_bundle/into_bundle/unload/6 mtp exits), mtp_weights_present() exact incl EP/PP+deepseek4 None cases, byte-identical. Minors: (1) stale "five pieces" comments — FIXED+amended (spec_build.rs, speculative.rs). (2) PRE-EXISTING GPU leak at daemon.rs cvs-scratch alloc-fail returns (7038/7042 no m.state restore) — my change adds head-local drop there; OUT OF SCOPE, needs bjoern decision.
- Step E COMPLETE. HEAD 36660547 (amended from 248f1ab1). Tree: only .superpowers/sdd/task-5-report.md (stale, pre-existing) + ledger untracked.
- Leak fix (bjoern-approved follow-up): commit 7e1aa7c2 — the 2 cvs-scratch alloc-fail exits in generate_qwen35_mtp now free state + restore m.state (re-pack like the other 6), fixing pre-existing bundle-loss + new head-drop. Separate commit (relocation stays byte-identical). Workspace build + lib tests green. OOM-only path (no gate coverage needed).
- STEP E FULLY DONE. HEAD 7e1aa7c2. Next: Step F (pp_gpus/pp_scratch_set/pp_dn_la_to_device → Qwen35Bundle/Qwen35PpState; disjoint-borrow fork in reset_qwen35_recurrent — brainstorm first).

## ═══ SESSION CORRECTION (2026-07-11): Axis B is DONE, not "NOT started" ═══
The 2026-07-10 axis-B handover/plan/ledger saying "NOT started" are STALE. Axis B (ds4-EP + minimax-EP onto ar_generate) SHIPPED: commits 7652570f(inc1 ForwardCtx)..15fb81b0(inc5 FLIP ds4-EP+delete ep_serve_ds4)..b82404d7(DONE)..e53a5f92(minimax-EP flip). Legacy ep_serve_ds4/ep_serve_minimax deleted; dual-run scaffold removed post-flip; final-reviewed (e6e1629f C1/I1/I2). Dense TP/PP also flipped (027a7237/f67ea948) → the entire ModelParallel DRIVER collapse is complete. Verified this session: ds4-EP coherent end-to-end via the example (Paris/Eiffel Tower).
ONE loose end fixed: the ep_deepseek4 D2a FNV anchor was stale (0x6c0f2f→0x26a1360, drifted very early post-pin, output coherent, decomposition byte-identical) → re-pinned + GPU-confirmed PASS.
REMAINING god-struct FIELD collapse (separate track from the driver unification): ModelParallel STRUCT grouping (7 fields) + ImmutableMeta. See godstruct-collapse-handover-2026-07-11.md.

## ═══ ModelParallel enum collapse (plan 2026-07-12) ═══
Plan: docs/superpowers/plans/2026-07-12-modelparallel-enum-collapse.md (7 tasks, per-axis atomic, byte-identical)
Task 1: complete (commits c3ac4814 fold qwen35 PP scratch→Qwen35Bundle.pipeline; +83ce1525 review-comment fix; review Spec✅ Approved, 1 Important=doc-only asymmetry resolved by comments to preserve byte-identity)
Task 2: complete (commit 3e99918c define ModelParallel/PipelineImpl + kind()/is_pipelined() + inert parallel=Single; review Spec✅ Approved, zero issues; Gpus type = hipfire_runtime::multi_gpu::Gpus, single type confirmed)
Task 3: complete (commit 8c3d7f85 migrate TP axis onto ModelParallel::Tp + daemon-side dense_model_mut, Pp(Dense) arm commented for T4; bench-guard temp clause preserves reject set; review Spec✅ Approved, 2 no-action Minors; emulated-TP tp=2 byte-identical 20-tok)
Task 4: complete (commit a4211e3c migrate dense-PP onto Pp(PipelineImpl::Dense); Pp(Dense) arm uncommented; fixed unload double-mem::replace hazard→single replace+match; is_pp dropped (field-selector only, no behavioral use); pp:mesh.size_of LEFT for T6; review Spec✅ Approved; CONTROLLER-ran gates: pp_unload_reload PASS + llama_store_pp max|Δ|=0 tok33450)
  CARRY→T7: (a) TP+PP gate arms already FUSED as `if matches!(kind(), Tp|PpDense)`; T7 finalizes EP arm + full match + bench. (b) model_parallel.rs ModelParallelKind doc block (~L8-47, variant docs) has STALE m.tp/m.pp_dense field refs + old dispatch line numbers → clean up in T7.
Task 5: complete (commit a4583dbc migrate EP axis onto ModelParallel::Ep; all 17 m.ep sites flipped byte-identical, unload single-replace+match EP arm, pub ep removed; review Spec✅ Approved 95% confidence)
  ⚠ EP-VALIDATION-GAP (flag to bjoern, for final review): end-to-end EP token-gen NOT runnable on this box. Daemon emulated EP-2 ds4 serve executed the FULL migrated path (load_model_ep→ModelParallel::Ep→generate_ep→EpState destructure→forward_ep→moe-step L0→execute_steps_parallel) then died at RcclComms::init_all — librccl.so NOT installed. ep_deepseek4 example defaults tp=4 → 4 emulated ranks OOM. Both blockers are UNTOUCHED code + env (would hit pre-Task5 baseline identically). Migration proven to WIRE correctly to the RCCL boundary; full EP token-gen needs librccl (apt install rccl / /opt/rocm/lib/librccl.so.1) OR real multi-GPU.
  CARRY→T7 cleanup: stale daemon.rs doc comments m.ep.is_some@2001, m.ep.gpus@5224 (+ Task3/4 stale m.tp/m.pp_dense refs + model_parallel.rs ModelParallelKind doc block).
Task 6: complete (commit 0fe02058 migrate qwen35 arch-resident PP onto Pp(ArchResident); DELETED pp+pp_gpus fields + desync sentinel; is_pipelined() derivations; teardown single move-out correct free order; review Spec✅ Approved all-6-risks-clean; GPU: qwen35 pp=2 emulated serve coherent(France) + serve-multiturn PASS)
  CARRY→T7 cleanup adds: daemon.rs:7511 stale `pp_gpus.devices[0]` comment.
Task 7: complete (commit 79921b33 dispatch match m.parallel.kind() + bench_reject equivalence test + priority() dropped + stale doc sweep; reviewed INLINE by controller [Task 7 impl was slow not dead]: dispatch byte-identical [same args/order/fall-through], no priority() callers, no stale field comments, build green, all workspace tests pass, bench test GREEN, qwen35 pp=2 serve coherent [independent controller run], minimax ep=2 routes to generate_ep→librccl as expected; Approved)
ALL 7 TASKS COMPLETE. Commits: c3ac4814(+83ce1525) 3e99918c 8c3d7f85 a4211e3c a4583dbc 0fe02058 79921b33. Branch 0fe02058→79921b33 tip.

## ═══ ModelMeta collapse (plan 2026-07-12, final immutable-scalar step) ═══
Plan: docs/superpowers/plans/2026-07-12-modelmeta-collapse.md (2 tasks; T1 additive dual-home, T2 flip+delete). Spec: 2026-07-12-immutablemeta-collapse-design.md (ModelMeta, eos unified, +max_seq/physical_cap, splitter returns &meta, 3 load-time writers). BASE 79921b33.
MM-Task 1: complete (commit 8be7bf63 define ModelMeta + populate dual-home + session_parts_mut→3-tuple(&ModelMeta); build+966 tests green). Reviewed INLINE by controller — the critical DUAL-HOME DIVERGENCE hazard verified clean: skeleton meta≡flat (L445-459), 4 explicit-meta sites tp/pp/ep_ds4/ep_minimax mirror flat overrides (eos+rec_*+chat_template), rec_* finalize sets BOTH (1231-1236), mtp handler sets BOTH (3668-3671), cohere2moe(1268)+generic carrier(1072) pass overrides through skeleton params. No divergence. meta dead_code warning expected (T2 adds readers).
MM-Task 2: complete (commit 9c57148d flip ~100 readers→m.meta, unify eos→m.meta.eos_tok [if arch_id==10 collapsed], delete 13 flat fields+dual-writes; +dspark_qwen3_1window.rs compiler-guided flip). Reviewed INLINE by controller: LoadedModel=EXACTLY 9 fields (parallel/state/vision/tokenizer/eviction/session/persist/speculator/meta), ZERO flat survivors (grep clean), build green no meta dead_code, serve-multiturn+coherence PASS. Approved.
✅✅ GOD-STRUCT FIELD COLLAPSE COMPLETE @ 9c57148d. ModelMeta commits: 8be7bf63, 9c57148d.
