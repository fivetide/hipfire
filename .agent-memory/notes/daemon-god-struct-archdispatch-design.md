---
title: Daemon god-struct collapse → ModelParallel + ArchDispatch (design APPROVED, pre-impl) — the one structural unification independent of Step-decomposition
date: 2026-07-09
tags: [device-mesh, god-struct, loadedmodel, archdispatch, modelparallel, sessionstate, 462, state-bleed, daemon, dispatch, refactor, design, phase3, trait-object, parity-harness]
---

> **Historical document.** This file preserves dated implementation and validation evidence. Current status and remaining work are tracked only in [device-mesh-refactor-tracker.md](../../.agent-progress/device-mesh-refactor-tracker.md).

**Branch:** `feature/device-mesh` (worktree `.claude/worktrees/feature+device-mesh`). Full spec (gitignored-local per branch convention): `docs/superpowers/specs/2026-07-09-daemon-god-struct-archdispatch-design.md`. Parent: [[device-mesh-pivot-execute-steps-spine]] ("the one structural unification still worth doing"). **Independent of** the MoE/DeltaNet Step-decomposition thread (P-D/P-E): that delivers parallelism *transparency*; this collapses the daemon dispatch god-struct + makes #462 state-bleed structurally impossible.

## Decisions locked (bjoern, 2026-07-09)
- **Goal = BOTH, sequenced:** kill the #462 bleed surface AND collapse the god-struct; **#462-safety is the acceptance test** gating every increment.
- **Approach = C (full `Box<dyn ArchDispatch>` trait object), design-full / land-incremental.** Realized as **shared driver + hooks**, NOT a monolithic `fn ar_generate()` (the anatomy mapper flagged a single opaque method with per-phase if-chains as worse than per-arch hook impls). Every arch behind one `dyn ArchDispatch`; the daemon keeps ONE `ar_generate` driver owning the arch-invariant phases.
- **Inc 1 arch = qwen35 DeltaNet** (the catastrophic #462 case — recurrent state + DFlash checkpoints; prove the hardest lifecycle first).
- **Parity harness = dual-run env-gated shadow assert** (`HIPFIRE_ARCHDISPATCH_PARITY=1` runs old+new loops per request, asserts token-identical, off in prod). The safety spine that lets C land without a big-bang cutover.

## Target shape (~50 fields → ~6)
```rust
struct LoadedModel {
    parallel:   ModelParallel,          // where: was pp/pp_gpus/pp_scratch_set/pp_dn_la_to_device/ep/tp/pp_dense
    arch:       Box<dyn ArchDispatch>,  // what:  was state(ModelState)+arch_id+*_eos_tok+mtp_head+dots/vision+rec_* ladder+feature wiring
    session:    SessionState,           // request: was seq_pos/conversation_tokens/prefill+dflash_checkpoints/asst_turn_cache/kv_adaptive/eviction
    speculator: Option<Box<dyn Speculator>>,  // unchanged (already clean)
    tokenizer, model_path, chat_template,     // immutable infra
}
enum ModelParallel { Single, Tp(TpModel), PpDense(PpModel), PpQwen35{..}, Ep(EpState) }
```
`ArchDispatch` = AR-decode analog of load-time `Carrier` + TP/PP `DenseServed`. Hooks: `arch_id/eos_token/sampling_defaults/features/reset` + AR phases `frame_and_tokenize/prefill(lcp)/forward_step/stream_parser()->Option/finalize` + `as_spec_target()->Option<&mut dyn SpecTarget>` (bridges the EXISTING spec seam; SpecTarget/Speculator/ModelState/Carrier/DenseServed all stay, ArchDispatch composes).

## #462 mechanism (why bleed becomes unrepresentable)
Today reset is spread across **5+ sites** (per-handler overflow guard, `reset` cmd, VL entry guard, decode abort, EP abort), each an `if let Some(ModelState::Arch(..))` chain a new arch must remember to touch — correctness by vigilance. Target: ONE `LoadedModel::reset_context(gpu)` = `session.clear()` (**total by ownership** — a field not in SessionState is config or arch-owned) + `arch.reset(gpu)` (**total by the compiler** — impl ArchDispatch forces a reset arm) + speculator.reset. Adding an arch can't skip a reset. Guard = `serve-multiturn-gate.sh`.

## Increment plan (each gated: parity-shadow token-identical + serve-multiturn + coherence + build/test)
- **Inc 0:** new types alongside existing fields (dual-state, zero behavior change); `ar_generate` written unrouted.
- **Inc 1:** qwen35 (5/6) through `ar_generate`+ArchDispatch behind shadow-parity. Extracts the embedded qwen35 AR loop from `generate()` body (~daemon.rs:8588) — highest risk, done first.
- **Inc 2..N:** one arch/increment, delete old loop when green. Order: qwen2 → llama(+DenseServed axis seam) → minimax/cohere2/lfm2moe → deepseek4(DSML StreamParser) → qwen35-MTP → VL(qwen35-vl, dots-ocr).
- **Inc N+1:** ModelParallel collapse — axis-first tree → `match model.parallel`; migrate `load_model_{tp,pp,ep}`. Sequenced LAST to avoid colliding with live D-series load-path work; rebase on device-mesh HEAD first.
- **Inc N+2:** delete migrated direct fields + arch_id ladders + split *_eos_tok → 6-field target.

## Grounding (3 read-only subagent maps, 2026-07-09)
- **God-struct:** `LoadedModel` `crates/hipfire-loader/src/lib.rs:273`, ~50 fields. `generate()` `daemon.rs:6851` axis-first→arch-second, **25+ arch_id match sites** (sampling ladder ~1807-1881, EOS ~3079, feature wiring w/ silenced-unused-param warts).
- **Loops:** 9 AR paths, **12-phase skeleton, structurally isomorphic / operationally divergent**; variance concentrated in phases 3/5/9-10 (LCP/prefix-cache, prefill, per-token sample+forward) — ds4 DSML parser, minimax partial-LCP, qwen2 greedy-only. Two loops EMBEDDED in `generate()` (qwen35 ~8588, llama ~9734).
- **#462 today:** NO active bleed (all sites currently reset correctly) — the win is removing the *latent* surface (vigilance→construction), not fixing a live bug.
- **Existing seams:** `Carrier` `loader_api.rs:115`; `SpecTarget`/`Speculator` `spec.rs:155/582`; `ModelState` enum `lib.rs:242` (7 bundles: Qwen2/Qwen35/Llama/Lfm2Moe/Minimax/Cohere2Moe/Deepseek4).

## Non-goals / risks
Non-goals: NOT the Step-decomposition thread; NOT changing SpecTarget/Speculator/ModelState/Carrier/DenseServed; NOT a perf change (byte-identical target — any tok/s delta is a regression); NOT new functionality. Risks: (1) embedded qwen35/llama loop extraction entangled with axis dispatch (Inc 1 guard = parity assert); (2) hooks too coarse for ds4/minimax/qwen35-adaptive-KV → may add 1-2 optional hooks after Inc 1 + deepseek4; (3) AR-vs-spec routing must match today via `as_spec_target`; (4) Inc N+1 merge friction w/ D-series; (5) VL migrated last.

**Next:** `writing-plans` → Inc 0 + Inc 1 implementation plan.

## STATUS 2026-07-10 — Inc 0 DONE + Inc 1 Task 1.4 DONE (qwen35 through ar_generate)
Inc 0 complete (@1c78d297). Inc 1 Task 1.4 complete: expanded `ArchDispatch` (added tangle hooks maybe_evict/maybe_adaptive_downshift/take_prefill_checkpoint/abort_zero_recurrent/sample + GrammarMatcher::attractor_detected — the plan's "5 hooks" was insufficient; the "arch-neutral tangle" touches the arch bundle's kv/dn so it needs hooks too, and the runtime trait can't name LoadedModel so the dispatcher OWNS &mut m per Task 1.2). Generic `ar_generate` extracted (commits 380e7bea/036f07cf/e1d1b97a), dual-run shadow-parity wired (8a6b5dc7), GPU-parity PROVEN token-identical (5 prompts × single-GPU + emulated-2, FP32 DeltaNet + HIPFIRE_DETERMINISTIC=1), then flipped to prod + legacy arm deleted (−1212 lines), coherence-gate validated. Ledger: `.superpowers/sdd/progress.md`.

## INC 2..N ABSORPTION PROGRAM (bjoern 2026-07-10 "sequentially absorb all architectures") — live status
Goal: every arch's AR path onto the single ar_generate/ArchDispatch driver. UPLIFT decision (bjoern): migrated arches ADOPT ar_generate's richer feature set (n-gram loop guard, think-cap, stop-seqs, generic sampler) — NOT byte-identical to the simpler legacy loops; validate via clean-prompt/temp0 parity + coherence, not strict all-prompt parity. Sequence + status:
- **Inc 2 qwen2 (arch 7) DONE** (27bfadf3 Qwen2Dispatch + e1ce5a8b is_eos hook + c2ccf933 flip). Clean split port, ZERO new hooks (proved ar_generate generic). Added `is_eos` hook (per-arch stop set: qwen35=eos||terminator / qwen2=eos_token_ids config set). Coherent; qwen2 still feeds RAW prompt (pre-existing framing degeneration — [[vibethinker-framing-rootcause]], now fixable via ar_generate).
- **Inc 3 llama (arch 0/1) DONE** (0baca0b9 LlamaDispatch + 26be61d1 flip, path-a). llama arm was FUSED+minimal (forward_scratch samples internally, no features). Path-a: batched forward_prefill_batch B=1 + generic sampler. PERF-NEUTRAL A/B (0.6b 256→258, 8b 18.0→18.4, within noise — B=1 concern falsified). Coherent. NOT byte-identical (intentional uplift).
- **Inc 4 minimax (arch 10) DONE** (2026-07-10; ab6e29fe dual-run wiring → 638e7353 flip → 42d64251 eos-marker fix). MinimaxDispatch::sample downloads state.logits[GPU-resident]+sampler::sample_cpu (temp0=argmax parity PROVEN, temp>0 uplift); primed_think "<think>\n" re-emit moved to preamble BEFORE ar_generate (prefill now inside it); LCP partial-reuse preamble kept, cached_tokens_count=prompt_ids.len−prefill_ids.len; ar_generate prefills the post-LCP suffix from seq_pos=MiniMaxState.n_tokens. **VALIDATION MODEL = `MiniMax-M2.7.mq2` (arch 10, 79GB, ~28 tok/s)** — the resume-note pointer `ornith-35b-aeon.mq6` was WRONG (that's `qwen3_5_moe`=arch 5/6 VL). Temp0 dual-run parity: 3 prompts, full 40/48/48-tok greedy, assert_token_parity never fired, decode 27.9 vs 27.87 tok/s. Prod coherence: 4 turns fluent, multi-turn LCP partial-reuse CONFIRMED under ar_generate (t2 lcp=49 cached=49 partial=true), is_eos fires (finish_reason=stop). **REGRESSION CAUGHT + FIXED (parity structurally missed it — those prompts hit max_tokens, never eos):** MiniMax's eos token IS `[e~[` which decodes to a LITERAL (not empty like ChatML `<|im_end|>`); legacy broke pre-emit, ar_generate commits+emits eos before its is_eos break → `[e~[` leaked into visible output. Fix = new `ArchDispatch::eos_filter_config()` hook (default empty pass-through = qwen/llama byte-identical; MinimaxDispatch stop_at `[e~[`); re-validated eos-terminated turn ends `...Berlin.` clean (0 markers). See follow-up #4.
- **RE-ORDERED (bjoern 2026-07-10, after scoping):** the plan's "cohere2moe medium, then lfm2moe" was BACKWARDS. cohere2moe's decode is a full agentic-marker STATE MACHINE (Sec::{Think,Text,Action} routing + marker suppression + empty-turn EOS-mask guard + think-budget force-close via a forced-token-injection queue + `<|START_ACTION|>`→structured tool_calls PARSING) — ar_generate has none of it and EosFilter can't emit tool_calls or feed tokens back. So cohere2moe needs a NEW StreamParser hook = the SAME class as ds4 DSML. lfm2moe is a plain AR loop (bare sample_token) + conv-state. **New order: lfm2moe (medium) NEXT; cohere2moe + deepseek4 = the StreamParser COHORT (build the hook ONCE, apply to both).**
- **Inc 5 → lfm2moe (arch 11) DONE** (2026-07-10; f5133030 dispatch+dual-run → 529b8c3a flip). Turned out SIMPLER than forecast: the feared conv-state recurrent hooks stay NO-OP because the legacy path COLD-RESETS state every turn (hybrid conv+GQA can't be rewound → no LCP, no checkpoints; post-abort partial state recovered by next turn's reset). Real work = the eos-marker family: LFM2 ends on `<|im_end|>`(=eos_tok) but ALSO `<|endoftext|>`/`</s>` whose ids DON'T round-trip through encode (legacy needed a decoded-frag string guard). Lfm2MoeDispatch carries a `stop_ids` field (resolved via special_token_id) for one is_eos check + eos_filter_config strips all 3 literals (same class as minimax `[e~[`, per [[archdispatch-eos-filter-per-arch-checklist]]). Per-token prefill (no batched). VALIDATED lfm2.5-8b-a1b.mq4: temp0 parity HELD (tapes identical incl terminal eos: legacy 79 emitted vs ar_generate 80 committed = the eos-commit delta), ar_generate arm 0 marker leaks, decode 158 vs 159 tok/s. That leaves the StreamParser COHORT as the only remaining AR arches.
- **StreamParser Axis A DONE (2026-07-10)** — spec `docs/superpowers/specs/2026-07-10-streamparser-output-unification-design.md`, plan `docs/superpowers/plans/2026-07-10-streamparser-output-unification.md`. `StreamParser` trait (`next_forced`/`on_eos`/`feed(tok,bytes)`/`finish`/`emit_only`/`enqueue`; `StreamAction{Emit{reasoning},ToolCalls,Info,Stop}`; `EosDecision{CommitAndStop,Stop,Inject}`) in runtime lib `stream_parser.rs`. `DefaultStreamParser` (byte-identical: EosFilter emit + think-cap-via-next_forced + n-gram + in-think budget-alert; budget-alert !in_think RE-SAMPLE stays driver-side) + `Cohere2MoeDispatch` + `Cohere2MoeStreamParser` (Sec marker machine + forced queue + empty-turn on_eos Inject + tool_calls + vis_buf recovery). ar_generate delegates ALL output to the parser; eos decision moved PRE-COMMIT (loop top) for the Inject discipline. Commits 2fc16d7b→0a635eb1. **VALIDATED:** 5-arch byte-identity RE-CONFIRMED after the driver eos-restructure (qwen35/lfm2moe cross-commit f9327987 vs HEAD, committed tok_ids+text identical; asst-turn-cache fingerprint intact cached_tokens=63); cohere2moe token-parity floor HELD (cap/code/sheep) + coherence-gate-cohere2moe.sh PASS (0 marker leaks, long-context OK) + guard fixtures (tool_calls emits valid {get_weather,Tokyo}, empty-turn coherent). **BUG CAUGHT+FIXED by the dual-run (cf754c74): model_reset_context had NO cohere2moe reset → stale n_tokens/KV → argmax flip; also a latent #462 cohere2moe reset-cmd bleed.** cohere2moe −467L legacy loop deleted.
- **StreamParser Axis B (NEXT increment): deepseek4 (arch 9)** — DEFERRED because its production path is EP (`ep_serve_ds4`/`forward_ep(gpus:&mut Gpus)`), which the single-`gpu` ArchDispatch hooks CANNOT carry (adversarial review + device-plumbing investigation, 2026-07-10). ds4 = the **ModelParallel collapse**: wire `ModelParallelKind` into the generate() gate, `ar_generate` dispatches on `ModelParallel{Single,Dense,Ep}` via `ForwardCtx{Single(&mut Gpu),Mesh(&mut Gpus)}`, fold `generate_dense`/`ep_serve_*`; then ds4 rides ar_generate with a `Deepseek4GrammarMatcher` (existing init_grammar hook) + a `Deepseek4StreamParser` (wrapping `deepseek4::dsml::StreamParser`). Big refactor — rebase on device-mesh HEAD first (load-path collision). The StreamParser trait + DefaultStreamParser are ready to receive it.
- **StreamParser Axis B (deepseek4-EP) DONE (2026-07-10, EP-ONLY scope; 6 commits `7652570f..<flip>`).** ds4 EP AR decode now rides the ONE `ar_generate` driver; `ep_serve_ds4` DELETED (−420L). Device access = **`ForwardCtx { Single(&mut Gpu), Mesh }` — Mesh is a UNIT marker, NOT `Mesh(&mut Gpus)`** (RETRACTS the design line above): the EP `Gpus` lives inside `m.ep`, the same `&mut m` the dispatch borrows, so a device-carrying Mesh variant is borrow-illegal (aliases `&mut self`); the EP dispatch reaches `m.ep.gpus` through `&mut self`, and `sample` widened `&self→&mut self`. `Deepseek4EpDispatch` (first mesh dispatch): per-token `forward_ep` prefill(loop chunk)/decode, per-rank reset, rank-0 `download_f32` + HOST `sample_full_dist` (NOT sample_cpu/GPU sampler). Output = `Deepseek4GrammarMatcher` (init_grammar rebuilds the FULL ds4 ToolSchema incl `params` from the dispatch-held raw `tools` — the trait `(name,required)` tuple drops params) + `Deepseek4StreamParser` (wraps dsml behind Option for self-consuming finish; **on_eos=Stop NOT CommitAndStop** — ep_serve_ds4 breaks pre-feed, eos never enters KV/tape/parser). Flip: generate_ep arch-9 = per-turn `ep_reset_ds4_state` (EP has no LCP) + `ep_serve_ds4_via_ar_generate` (30-arg call, im_end/think_pair empty=dsml owns think/tools). **VALIDATED (emulated EP-2, deterministic):** dual-run token-parity ep_serve_ds4==ar_generate+EP on capital/reasoning/code/tool-call; post-flip coherent + correct {get_weather,Tokyo}; multi-turn t1 Tokyo/t2 Berlin (no #462 bleed). **Two bugs the dual-run caught:** (1) `Deepseek4EpDispatch` missing `ensure_decoded_vocab` → grammar path panicked at the trait default `unimplemented!()` (non-grammar rows never call it → invisible until the tool-call row) — fix c4ab43c2; (2) `model_reset_context` does NOT reset `m.ep` (same class as the Axis-A cohere reset bug) — BENIGN here because generate_ep resets EP per-turn before every generation, but any future non-generate EP path must add it. **FNV anchor `DS4_EP2_FNV` is UNUSABLE on gfx1151** (pinned 0x6c0f2f000f1d398f vs box 0x26a13602bedf9926 — pre-existing baseline drift, deepseek4 crate byte-untouched; the dual-run is the correct oracle). NON-GOALS held: minimax-EP (`ep_serve_minimax` intact, shared ep helpers stay), dense TP/PP fold, spec-decode, VL.
- **minimax-EP fold DONE (2026-07-10 follow-on, bjoern chose it; 3 commits M1/M2/M3).** Second mesh arch onto `ar_generate`; `ep_serve_minimax` + the 4 orphaned `ep_emit_*` helpers DELETED (~530L — they only served the two folded EP loops, so ds4+minimax flip left them dead). `MinimaxEpDispatch` = the ds4 mesh shape (forward_ep/decode over `EpArch::Minimax`, rank-0 host `sample_full_dist`) but REUSES `MinimaxDispatch`'s OUTPUT hooks: `[e~[` eos filter + the **DefaultStreamParser** (plain text, no DSML/grammar — and the default parser already honors `cfg.stop_seqs`, so no I1-class fix needed here; it even passes real `stop`+`top_k`/`min_p`, better than the single-GPU minimax flip which passed `&[]`). KEY DIFF from ds4: minimax has **LCP** (prefix-cache) — `ep_serve_minimax_via_ar_generate` mirrors the single-GPU `generate_minimax` LCP preamble (rewind each rank's `n_tokens` to the common prefix, prefill the divergent suffix, cached_tokens_count=lcp) + the display-only `<think>` primer; NO per-turn full reset (unlike ds4). The dual-run needed no reset between arms — the LCP self-heals (arm-1's full-prompt prior tape → arm-2 cache miss → cold prefill → matches). Validated (emulated EP-2): dual-run parity capital/code; post-flip coherent single-arm + multi-turn Tokyo/Germany no-bleed. Both EP arches now ride ar_generate; `ep_serve_*` fully gone.
- **Dense TP/PP fold DONE (2026-07-10 follow-on, bjoern "fold dense"; 2 commits D1/D2) — the LAST separate multi-GPU driver.** `DenseDispatch` (THIRD mesh dispatch) wraps the `DenseServed` model (`TpModel` when `!is_pp` / `PpModel` when `is_pp`; `Gpus` in `m.tp`/`m.pp_dense` → `ForwardCtx::Mesh` via `&mut self`). Folds `generate_dense`: `prefill_forward` = batched `model.prefill` at pos 0 / per-token suffix on an LCP hit (`prefill_max_batch=MAX` so ar_generate hands the whole suffix as one chunk and the model batches internally ≤256); host `sample_cpu` over `logits()`; `is_eos = eos || is_terminator`; lean (no think/grammar/eviction/checkpoints). Dense models expose NO cursor → the shared `dense_serve_via_ar_generate` seeds `m.seq_pos` from the LCP `start_pos` and leaves `conversation_tokens == rendered[0..start_pos]` (truncate on a pure-extension hit, clear on a miss) so ar_generate's `extend(new_tokens)` + per-token push rebuilds `rendered + generated` (== the old gate's bake) for next-turn LCP. Both tp + pp_dense gates flipped; `generate_dense` + the dead `DenseServed::eos_token` deleted (`DenseServed` trait kept for the `dyn` over Tp/Pp). VALIDATED: CROSS-COMMIT text-identity (emulated, temp0) pp=2 + tp=2 `old(generate_dense) == new(ar_generate)` → fold byte-preserving; coherent. TRAP for whoever validates dense: pp=1 (single-GPU llama) ≠ pp=2 (dense) at the daemon level is EXPECTED (different render/sample paths — `tp_decode_parity`/`pp_decode_parity` validate FORWARD math, not daemon-serve equivalence); the fold oracle is cross-commit vs `generate_dense`, NOT pp=1-vs-pp=2. `pp-gate.sh` needs 2 PHYSICAL GPUs (skips under `HIPFIRE_EMULATE_GPUS`). **ModelParallel collapse DONE at the driver level: ALL AR paths — 6 single-GPU arches + 3 mesh (ds4-EP, minimax-EP, dense TP/PP) — now flow through the one `ar_generate`; `ep_serve_*` + `generate_dense` all deleted.** Remaining structural work (separate): the `LoadedModel` god-struct field collapse into a `ModelParallel` enum (the fields `tp`/`pp_dense`/`ep` still coexist as `Option`s).
- **DEFER**: VL (dots-ocr@13288 / qwen35-vl@12678 image embeds) + all spec-decode (generate_spec/_dflash/_deepseek4_spec — the "fold spec into ArchDispatch" follow-up #2 below).
PATTERN per arch: fork implements <Arch>Dispatch + dual-run parity wiring (build-only) → controller GPU-validates (temp0/clean parity + coherence + tok/s A/B) → flip (delete legacy loop) → commit. Separate-fn arches keep their own preamble + call ar_generate with its outputs (pflash_*=None). No trait changes needed so far except is_eos (Inc 2). CLEANUP: generate()-level `pflash_done_fragment`@daemon.rs:9478 is now orphaned dead-code (qwen35 flip) — remove/underscore.

## FOLLOW-UPS surfaced during Inc 1 (bjoern 2026-07-10 — do not forget)
1. **Grammar hook cross-arch adoption.** `ArchDispatch::init_grammar → Option<Box<dyn GrammarMatcher>>` (+ `GrammarMatcher{token_mask/advance/is_free/attractor_detected}`) is qwen35-ONLY today (newtype `Qwen35GrammarMatcher` over `qwen35::grammar::Matcher`). deepseek4 has its OWN grammar Matcher (`deepseek4::grammar::Matcher`, DSML tool-call path); qwen2 tool-calls too. When those arches move onto `ar_generate` (Inc 2..N), wire each arch's Matcher through `init_grammar` via a per-arch newtype so grammar is arch-generic in the driver (no per-arch grammar branch). ACTION: inventory which arches have grammar/tool-call Matchers, unify under the hook.
2. **Expand ArchDispatch beyond AR — fold in speculation.** Inc 1 deliberately scoped ArchDispatch to the AR arm ONLY; spec-decode stays on separate loops (`generate_spec` qwen35 DFlash / `generate_deepseek4_spec` DSpark-MTP), and `as_spec_target()` is stubbed `None` because the qwen35 SpecTarget is GUARD-based RAII (`Qwen35Carrier::spec_target_guard → Box<dyn SpecTargetGuard>`, moves the bundle out of m.state + restores on Drop) which does NOT fit the trait's `Option<&mut dyn SpecTarget>`. FUTURE INCREMENT: reshape the trait's spec bridge to yield the guard, OR route the spec loop through ArchDispatch, so ONE generate driver handles AR + spec generically (unified-generate endgame). This is the real payoff of `as_spec_target`.
4. **eos-marker leak checklist per arch (Inc 4 finding — MANDATORY for every remaining flip).** ar_generate commits+emits the eos token through EosFilter BEFORE the is_eos break; the legacy separate-fn loops broke pre-emit. So any arch whose eos token decodes to a LITERAL string (not empty) leaks that marker into visible output. ChatML arches (qwen35/qwen2 `<|im_end|>`) decode eos→empty so the default EosFilter (empty pass-through) is fine; minimax `[e~[` did NOT and needed `eos_filter_config()` stop_at. **Before flipping cohere2moe/lfm2moe/deepseek4: check what its eos_tok decodes to** (grep `resolve_eos_tok`/loader eos candidates + decode it) and override `eos_filter_config()` if literal. Cohere2 especially — its whole point is `<|MARKER|>` state-machine tokens (`coherence-gate-cohere2moe.sh` HARD-FAILS on a marker leak), so budget an eos_filter_config override there. **Also structurally uncovered by temp0 parity: the eos path itself** — parity prompts that hit max_tokens never emit eos, so ALWAYS validate one eos-terminated turn (finish_reason=stop) on the prod path, not just parity.
3. **Port all other arches to Steps dispatch (SEPARATE thread — this note's non-goal).** `execute_steps` (Step IR) reach is scoped to llama arch 0/1; minimax/lfm2moe/cohere2moe/dots-ocr/deepseek4-EP/qwen35-MoE+DeltaNet still on bare `execute_steps` or hand-blocks. Decompose MoE/DeltaNet into single Steps per the DIRECTION AGREED (split WHAT from HOW/WHERE). Tracked in [[moe-deltanet-decompose-into-steps]] + [[device-mesh-pivot-execute-steps-spine]]. ORTHOGONAL to ArchDispatch: ArchDispatch = decode-LOOP dispatch (which arch's generate); Steps = per-LAYER op dispatch (how a forward runs across the mesh). They compose — an arch can be on ar_generate AND still hand-block its forward until Step-decomposed.

**HANDOVER for the Axis B execution session:** `docs/superpowers/plans/2026-07-10-axis-b-handover.md` (fresh-session resume prompt + FNV command + the inc-1 device-access design fork).
