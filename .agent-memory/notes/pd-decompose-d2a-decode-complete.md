---
title: P-D-decompose D2a COMPLETE — minimax+ds4 DECODE pre-down (score-act/route/gate-up/activation) decomposed into Steps, byte-identical EP-2, decomposed path is now sole EP path
date: 2026-07-08
tags: [device-mesh, moe, p-d, decompose, d2a, execute_steps_parallel, ep, deepseek4, minimax, step-ir, byte-identical, score-activation, moe-activation, d2b-next]
---

**Branch:** `feature/device-mesh` (worktree `.claude/worktrees/feature+device-mesh`). D2a = `5ba5483a..740df38b` (8 commits). Parent: [[pd-decompose-d1-ep-complete]] + [[moe-deltanet-decompose-into-steps]] (`## COURSE CORRECTION 2026-07-07`). Spec/plan: `docs/superpowers/specs/2026-07-08-d2a-moe-predown-decompose-design.md`, `docs/superpowers/plans/2026-07-08-d2a-moe-decode-predown-decompose.md` (gitignored-local).

## What D2a delivered
D1 decomposed only the MoE **down + Ep collective** into Steps. D2a completes the DECODE path: the routed-expert **pre-down** (score-activation → route → gate/up → activation) now runs as fine-grained Steps for **minimax(10) + ds4(9)**, joining D1's down/combine/collective in the per-rank `Vec<Step>`. The decomposed path is the **SOLE EP path** (temporary `HIPFIRE_MOE_STEP_PREDOWN` toggle flipped on then removed — repo no-opt-in-gate policy). **Byte-identical** on emulated EP-2 (validated via a real ON/OFF A/B during dev, now a single-run pinned assert): minimax FNV `0x887c2e7717e9c3bf`, ds4 FNV `0x6c0f2f000f1d398f`. ds4 MTP-EP ACCEPT preserved (`mtp_forward_ep` shares `ds4_ep_moe_step`).

## New Step IR (crates/hipfire-dispatch/src/pipeline/steps.rs + families/moe.rs)
- `Step::ScoreActivation{scores, kind: ScoreActKind::{Sigmoid|SqrtSoftplus}}` → `launch_score_activation` (sigmoid=minimax, sqrt_softplus=ds4; in-place). NOTE sigmoid_f32 is `#[cfg(deltanet)]` but minimax's default feature graph keeps `hipfire-dispatch/deltanet` on in every serving build.
- `Step::MoeActivation{variant, gate, up, rot_out, inter, k_top}`, `MoeActivationVariant::{MinimaxFused{awq_scale:Option<&GpuTensor>} | Ds4ClampRotate{swiglu_limit:f32}}` → `launch_moe_activation`. **Pure rdna-compute `Gpu` methods, NO runtime dep** (the runtime free-fn `fused_silu_mul_rotate_mq_batched_for` only used the down weight to pick the awq branch — the kernel takes no weight). minimax: `fused_silu_mul_rotate_mq_[awq_]batched`; ds4: `deepseek4_silu_mul_clamp_f32_batched`(in-place gate) then `rotate_x_mq_batched`. **ds4 is 2 kernels + a swiglu CLAMP — NOT the minimax fused kernel** (the original spec's "one FusedMoeActivation variant" was falsified in adversarial review).
- `Step::MoeRoute` (bias-aware, pre-existing) reused for BOTH arches — **both route bias-aware** (`deepseek4_moe_topk_bias_aware_f32`); minimax is NOT renorm (spec's renorm claim was wrong). `IndexedMoeGemv{GateUp}` reused as-is.
- **same-`kind` collective guard** landed in `execute_steps_parallel` (debug_assert all AllReduce share the group `kind` resolved from the first — no-op today, blocks D2b's Tp+Ep 2D mis-reduce).

## Per-arch wiring
- **minimax** (`minimax_ep_moe_step`): input prep stays arch-direct (rmsnorm + `rotate_x_mq_for` input rotate + router GEMV); prefix `[ScoreActivation{Sigmoid}, MoeRoute, IndexedMoeGemv{GateUp}, MoeActivation{MinimaxFused}]` prepended in BOTH lloyd(DownResidual) and non-lloyd(DownExpanded+MoeCombine) arms with `None`/`false` collective/zero_before. **Shipped M2.7.mq2 HAS awq on down** → MinimaxFused dispatches `_awq_batched` (byte-identical to direct).
- **ds4** (`ds4_ep_moe_step`): route (sqrt_softplus + bias-aware topk / hash-route incl. host-gather fallback), `routed` bool, `mhc_pre`, `ffn_stub`, shared expert ALL stay arch-direct. Only gate_up+activation decompose → prefix `[IndexedMoeGemv{GateUp}, MoeActivation{Ds4ClampRotate{cfg.swiglu_limit}}]` in both is_hash(DownResidual) and non-hash(DownExpanded+MoeCombine) arms. `ds4_moe_gate_up_silu_rotate` helper DELETED (its two EP callers removed — the Steps replace it; double-run trap avoided). Uniform MQ2G256Lloyd → one `expert_refs` serves GateUp+Down.

## Scope boundary (what stayed arch-direct = "input prep", like rmsnorm before Step::Attend)
FFN-input rmsnorm, input FWHT rotate, ds4 `mhc_pre`(hyper-connections)+`ffn_stub`(fused rmsnorm-rotate), ds4 shared-expert stream, ds4 **hash-route** (host-gather fallback isn't Step-expressible), router GEMV. The Step stream covers the routed-expert CORE only. This is bounded + byte-identical while still handing D2b the two ops it must shard (gate_up column, down row).

## Validation notes / traps (recorded)
- **ds4 EP-2 needs `--no-dspark`** to fit the 131GB UMA under emulated 2-rank (both ranks on device 0); `--mtp` still fits. `cfg.load_dspark:bool` @deepseek4.rs. The pinned ds4 FNV `0x6c0f2f00…` was RE-ESTABLISHED on-box (the spec's `0x316e0aa1` from a deleted harness never reproduced here; `0x6c0f2f00` matches the D1 ledger prefix).
- **Qwen `coherence-gate.sh`/`serve-multiturn-gate.sh` do NOT exercise the EP path** (single-GPU, DeltaNet). The real D2a gate is the EP-2 FNV byte-identity + workspace build. The entire toggle chain was EP-ONLY (single-GPU decode has its own gate_up @ds4 forward.rs:4175 / minimax `minimax_moe_block`) so the flip can't touch single-GPU.
- **ds4 recall gate hard-fails @depth=1500** = PRE-EXISTING gfx1151 long-ctx artifact on untouched single-GPU `decode_step_body`, NOT a D2a regression.
- PROCESS: a Task-7 subagent ran a forbidden bare `cargo fmt` (churned ~26 files), killed mid-run → recovered via `git checkout HEAD -- crates/`. Re-dispatch with hardened "scripts/fmt-changed.sh ONLY + pre-commit git-status self-check". (Same trap as D1 — see [[pd-decompose-d1-ep-complete]].)

## D2b = TP-of-experts (next, greenfield)
Slices the D2a Steps: `IndexedMoeGemv{GateUp}` column-parallel, down row-parallel. Two carry-forward constraints (verified this cycle): (1) per-expert column slice must be **256-group-aligned** (the FWHT rotate in MoeActivation is block-diagonal per-256 → shards on-rank with no all-gather; `fused_silu_mul_mq_rotate.hip:33` `groups_total=K/256`); (2) the fused gate‖up weight is `[2*inter, hidden]` block-concatenated → the column cut must take the **matching intermediate range for gate AND up** (paired/strided), not a contiguous cut of the concat. Also needs composite `ShardPolicy` (ExpertSharded×{Column|Row}) forcing new arms in collective_for_policy/placement_devices/validate_manifest/fulfill_into, kernel tp_rank/tp_size, combine-time Tp all-reduce, and per-step group resolution in execute_steps_parallel (the guard now flags it). **D2c = prefill-EP** (deferred — no valid single-GPU grouped oracle: minimax forward_batch is indexed not grouped + can't run shipped MQ3L; ds4 grouped threshold-gated; needs new batched EP scratch + daemon forward_prefill_batch_ep).
