=== pids before cleanup ===


============================ ROCm System Management Interface ============================
===================================== KFD Processes ======================================
KFD process information:
PID 	PROCESS NAME	GPU(s)	VRAM USED	SDMA USED	CU OCCUPANCY	
1823	gpuagent    	0     	0        	0        	0           	
==========================================================================================
================================== End of ROCm SMI Log ===================================


============================ ROCm System Management Interface ============================
================================== Memory Usage (Bytes) ==================================
GPU[0]		: VRAM Total Memory (B): 205822885888
GPU[0]		: VRAM Total Used Memory (B): 299687936
==========================================================================================
================================== End of ROCm SMI Log ===================================
=== hipfire/daemon processes ===
message+    1312       1  0 08:22 ?        00:00:01 @dbus-daemon --system --address=systemd: --nofork --nopidfile --systemd-activation --syslog-only
root       51234   51183  0 12:48 ?        00:00:00 bash -c set -euo pipefail E=/mnt/scratch/hipfire-evidence/ds4-mi300x-agentmaxx/g0-build WT=/mnt/scratch/hipfire-work/ds4-mi300x-agentmaxx cd "$WT" source /root/.cargo/env  # ownership / leftover processes {   echo "=== pids before cleanup ==="   rocm-smi --showpids || true   rocm-smi --showmeminfo vram || true   echo "=== hipfire/daemon processes ==="   ps -ef | grep -E "[d]aemon|[h]ipfire" || true   echo "=== daemon.pid ==="   cat /root/.hipfire/daemon.pid 2>/dev/null || true   ls -la /root/.hipfire/bin/daemon   sha256sum /root/.hipfire/bin/daemon   sha256sum $WT/target/release/examples/daemon   sha256sum $WT/target/release/hipfire } | tee $E/06-daemon-identity.txt  # If a leftover hipfire daemon is holding VRAM, stop it gently (not gpuagent) if [ -f /root/.hipfire/daemon.pid ]; then   PID=$(cat /root/.hipfire/daemon.pid || true)   if [ -n "${PID:-}" ] && kill -0 "$PID" 2>/dev/null; then     COMM=$(ps -p "$PID" -o comm= || true)     echo "pidfile pid=$PID comm=$COMM"     if [ "$COMM" = "daemon" ] || [ "$COMM" = "hipfire" ]; then       echo "sending TERM to leftover $COMM pid=$PID"       kill -TERM "$PID" || true       sleep 2       if kill -0 "$PID" 2>/dev/null; then         kill -KILL "$PID" || true       fi     else       echo "REFUSING to kill non-daemon pid $PID comm=$COMM"     fi   fi fi # also pkill exact name daemon only if not gpuagent pkill -x daemon 2>/dev/null || true sleep 1 rocm-smi --showpids || true rocm-smi --showmeminfo vram || true  export HIPFIRE_DAEMON_BIN="$WT/target/release/examples/daemon" echo "HIPFIRE_DAEMON_BIN=$HIPFIRE_DAEMON_BIN" | tee -a $E/06-daemon-identity.txt  echo "=== 06a RETRY with fresh daemon, NO WMMA workaround $(date -u) ===" | tee $E/06a-meta.txt T0=$(date +%s) set +e HIP_VISIBLE_DEVICES=0 HIPFIRE_LOCAL=1 HIPFIRE_SPECULATION=off HIPFIRE_DAEMON_BIN="$HIPFIRE_DAEMON_BIN" \   timeout 3600 ./target/release/hipfire run \   /mnt/scratch/models/deepseek-v4-flash-mq2r/deepseek-v4-flash.mq2r \   --temp 0 --max-tokens 32 --no-stream -- \   "Explain what a GPU kernel is in two sentences." \   > $E/06a-load-smoke-nowa.stdout 2> $E/06a-load-smoke-nowa.stderr RC=$? set -e T1=$(date +%s) echo "RC_06a=$RC ELAPSED=$((T1-T0))s" | tee -a $E/06a-meta.txt echo "=== stdout ===" | tee -a $E/06a-meta.txt cat $E/06a-load-smoke-nowa.stdout | tee -a $E/06a-meta.txt echo "=== stderr wc ===" wc -c $E/06a-load-smoke-nowa.stdout $E/06a-load-smoke-nowa.stderr echo "=== stderr full or tail ===" if [ $(wc -c < $E/06a-load-smoke-nowa.stderr) -lt 20000 ]; then   cat $E/06a-load-smoke-nowa.stderr else   tail -n 200 $E/06a-load-smoke-nowa.stderr fi echo "=== greps ===" grep -nE "drafter=ar|fallback|WMMA|wmma|error|Error|JIT|cannot|Cannot|missing|builtin_amdgcn|ineligible|portable" $E/06a-load-smoke-nowa.stderr | head -n 100 || true rocm-smi --showmeminfo vram 2>&1 | tee $E/06a-vram-after.txt rocm-smi --showpids 2>&1 | tee -a $E/06a-vram-after.txt echo "06A_RETRY_DONE" 
root       51235   51234  0 12:48 ?        00:00:00 bash -c set -euo pipefail E=/mnt/scratch/hipfire-evidence/ds4-mi300x-agentmaxx/g0-build WT=/mnt/scratch/hipfire-work/ds4-mi300x-agentmaxx cd "$WT" source /root/.cargo/env  # ownership / leftover processes {   echo "=== pids before cleanup ==="   rocm-smi --showpids || true   rocm-smi --showmeminfo vram || true   echo "=== hipfire/daemon processes ==="   ps -ef | grep -E "[d]aemon|[h]ipfire" || true   echo "=== daemon.pid ==="   cat /root/.hipfire/daemon.pid 2>/dev/null || true   ls -la /root/.hipfire/bin/daemon   sha256sum /root/.hipfire/bin/daemon   sha256sum $WT/target/release/examples/daemon   sha256sum $WT/target/release/hipfire } | tee $E/06-daemon-identity.txt  # If a leftover hipfire daemon is holding VRAM, stop it gently (not gpuagent) if [ -f /root/.hipfire/daemon.pid ]; then   PID=$(cat /root/.hipfire/daemon.pid || true)   if [ -n "${PID:-}" ] && kill -0 "$PID" 2>/dev/null; then     COMM=$(ps -p "$PID" -o comm= || true)     echo "pidfile pid=$PID comm=$COMM"     if [ "$COMM" = "daemon" ] || [ "$COMM" = "hipfire" ]; then       echo "sending TERM to leftover $COMM pid=$PID"       kill -TERM "$PID" || true       sleep 2       if kill -0 "$PID" 2>/dev/null; then         kill -KILL "$PID" || true       fi     else       echo "REFUSING to kill non-daemon pid $PID comm=$COMM"     fi   fi fi # also pkill exact name daemon only if not gpuagent pkill -x daemon 2>/dev/null || true sleep 1 rocm-smi --showpids || true rocm-smi --showmeminfo vram || true  export HIPFIRE_DAEMON_BIN="$WT/target/release/examples/daemon" echo "HIPFIRE_DAEMON_BIN=$HIPFIRE_DAEMON_BIN" | tee -a $E/06-daemon-identity.txt  echo "=== 06a RETRY with fresh daemon, NO WMMA workaround $(date -u) ===" | tee $E/06a-meta.txt T0=$(date +%s) set +e HIP_VISIBLE_DEVICES=0 HIPFIRE_LOCAL=1 HIPFIRE_SPECULATION=off HIPFIRE_DAEMON_BIN="$HIPFIRE_DAEMON_BIN" \   timeout 3600 ./target/release/hipfire run \   /mnt/scratch/models/deepseek-v4-flash-mq2r/deepseek-v4-flash.mq2r \   --temp 0 --max-tokens 32 --no-stream -- \   "Explain what a GPU kernel is in two sentences." \   > $E/06a-load-smoke-nowa.stdout 2> $E/06a-load-smoke-nowa.stderr RC=$? set -e T1=$(date +%s) echo "RC_06a=$RC ELAPSED=$((T1-T0))s" | tee -a $E/06a-meta.txt echo "=== stdout ===" | tee -a $E/06a-meta.txt cat $E/06a-load-smoke-nowa.stdout | tee -a $E/06a-meta.txt echo "=== stderr wc ===" wc -c $E/06a-load-smoke-nowa.stdout $E/06a-load-smoke-nowa.stderr echo "=== stderr full or tail ===" if [ $(wc -c < $E/06a-load-smoke-nowa.stderr) -lt 20000 ]; then   cat $E/06a-load-smoke-nowa.stderr else   tail -n 200 $E/06a-load-smoke-nowa.stderr fi echo "=== greps ===" grep -nE "drafter=ar|fallback|WMMA|wmma|error|Error|JIT|cannot|Cannot|missing|builtin_amdgcn|ineligible|portable" $E/06a-load-smoke-nowa.stderr | head -n 100 || true rocm-smi --showmeminfo vram 2>&1 | tee $E/06a-vram-after.txt rocm-smi --showpids 2>&1 | tee -a $E/06a-vram-after.txt echo "06A_RETRY_DONE" 
root       51236   51234  0 12:48 ?        00:00:00 tee /mnt/scratch/hipfire-evidence/ds4-mi300x-agentmaxx/g0-build/06-daemon-identity.txt
=== daemon.pid ===
50762
-rwxr-xr-x 1 root root 25138744 Aug  1 08:47 /root/.hipfire/bin/daemon
f99f53d81ea6e580a9fed50e09b5ec8e1d8001d7bb56cc1a11a361255ff0c7d4  /root/.hipfire/bin/daemon
6207b9a3fe2be4a7719acb34b992d8e7ca84b965fa83166cff0aeca9ed10bc67  /mnt/scratch/hipfire-work/ds4-mi300x-agentmaxx/target/release/examples/daemon
c1a99f7bd359ac435a1169ba25d85096761a0c757c3c96f6b3381a948acc88d6  /mnt/scratch/hipfire-work/ds4-mi300x-agentmaxx/target/release/hipfire
HIPFIRE_DAEMON_BIN=/mnt/scratch/hipfire-work/ds4-mi300x-agentmaxx/target/release/examples/daemon
