: A single mismatch between the intended target (gfx942) and the runtime device type, or between the intended model (DeepSeek V4 Flash) and the loaded model, invalidates the entire measurement. The same applies to the KV mode, the token count, the temperature, and the absence of speculation. The measurement contract is not a negotiation; it is a binary condition. The operator must verify that the runtime reports the expected device type, model name, and configuration before accepting any timing data. If the runtime silently falls back to a different architecture, a different quantization scheme, or a different execution path, the measurement is invalid regardless of whether the output appears correct.

The same principle applies to the numerical comparison. The operator must compare the output token sequence byte-for-byte with the reference output produced by the reference implementation under identical conditions. If the output differs, the measurement is invalid regardless of the timing. The operator must not attempt to "fix" the output by adjusting parameters or applying post-processing. The measurement is a measurement of the product path, not of a modified or corrected version of it.

The operator must also verify that the output is produced by the same process that produced the reference output. If the process is different (e.g., a different version of the software, a different compiler, a different set of dependencies), the measurement is invalid. The operator must ensure that the process is identical in every relevant way, including the version of the operating system, the version of the runtime, the version of the model, and the version of the dependencies.

The operator must also verify that the output is produced under the same conditions as the reference output. If the conditions are different (e.g., a different temperature, a different top-p, a different seed, a different number of tokens), the measurement is invalid. The operator must ensure that the conditions are identical in every relevant way, including the temperature, the top-p, the seed, the number of tokens, and the number of iterations.

The operator must also verify that the output is produced by the same hardware as the reference output. If the hardware is different (e.g., a different GPU, a different CPU, a different memory configuration), the measurement is invalid. The operator must ensure that the hardware is identical in every relevant way, including the GPU model, the CPU model, the memory size, and the memory speed.

The operator must also verify that the output is produced under the same environment as the reference output. If the environment is different (e.g., a