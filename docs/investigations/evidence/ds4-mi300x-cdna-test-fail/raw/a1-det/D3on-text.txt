: The architecture must match the declared target. A binary compiled for gfx942 must not silently fallback to a different device or a different instruction path. The runtime must reject any mismatch with a clear error and not silently run on a different device. The same applies to the model type: a DeepZ model must not be loaded as a different model type. The binary and model must be the same across all runs. The same applies to the kernel path: a candidate kernel must not fall back to a different kernel path. The same applies to the quantization format: a Q8 KV must not fall back to a different quantization format. The same applies to the compute precision: a FP16 compute must not fall back to a different compute precision. The same applies to the memory format: a Q8 KV must not fall inference with a different memory format. The same applies to the tensor layout: a Q8 KV must not inference with a different tensor layout. The same applies to the memory bandwidth: a Q8 KV must not inference with a different memory bandwidth. The same applies to the memory latency: a Q8 KV must not inference with a different memory latency. The same applies to the memory hierarchy: a Q8 KV must not inference with a different memory hierarchy. The same applies to the memory topology: a QQ8 KV must not inference with a different memory topology. The same applies to the memory topology: a Q8 KV must not inference with a different memory topology. The same applies to the memory topology: a Q8 KV must not inference with a different memory topology. The same applies to the memory topology: a Q8 KV must not inference with a different memory topology. The same applies to the memory topology: a Q8 KV must not inference with a different memory topology. The same applies to the memory topology: a Q8 KV must not inference with a different memory topology. The same applies to the memory topology: a Q8 KV must not inference with a different memory topology. The same applies to the memory topology: a Q8 QV must not inference with a different memory topology. The same applies to the memory topology: a Q8 KV must not inference with a different memory topology. The same applies to the memory topology: a Q8 KV must not inference with a different memory topology. The same applies to the memory topology: a Q8 KV must inference with the same memory topology. The same applies to the memory topology: a Q8 KV must inference with the same memory topology. The same applies