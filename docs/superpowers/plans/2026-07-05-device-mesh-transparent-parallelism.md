# Engine-level transparent PP/TP/EP — North-star design + phased roadmap

> **SUPERSEDED — historical (2026-08-26 mainline merge).** This plan's
> `resolve_mesh` / `resolve_parallelism` routing design and its
> `HIPFIRE_PP`-via-Bun-CLI text describe the pre-merge feature branch. In the
> merged mainline both precedence helpers were deleted: non-legacy load
> requests are preflighted by `hipfire_loader::admit_path`
> (`daemon_load_plan`; refusals surface before any unload), and only admitted
> dense TP/PP continues through `load_admitted`, which routes the mesh via
> `select_load_mesh` (`crates/hipfire-loader/src/lib.rs`; `effective_load_mesh`
> is the normalized-admission branch), served end-to-end via the MeshCarrier
> route (`tp_model` / `pp_model`, HFQ-only). EP loads
> (`load_model_ep_with_kv_mode`), single-device loads
> (`load_model_with_gemma4_drafter`), and the legacy non-VL Qwen3.5 HFQ
> `pp>1` exemption keep their own continuations; `params.ep` folds into the
> admitted parallel request; `HIPFIRE_EMULATE_GPUS` stays env-only emulation
> with no auto-promotion. Keep as provenance; current semantics live in
> [`docs/multi-gpu.md`](../../multi-gpu.md).

> Revised after a 3-reviewer adversarial pass (correctness, completeness, phasing). Findings integrated; two reviewer claims rejected with evidence (noted inline); the "maximal transparency" ambition kept but made reachable via explicit prerequisite phases.

> ⚠️ **PIVOT (2026-07-06) — READ `## PIVOT` BELOW FIRST.** The master merge (`5b95cbd3`, 519 commits) landed master's new `dense_forward`→`execute_steps` dense spine (commit `2a41f98f`) and **reverted** this branch's `3a3c60e5` (qwen2 → `run_layer_program_mesh`). The plan's central §1 thesis — "ONE executor = `run_layer_program(mesh, gpus, …)`" on the `SuperOp`/`ForwardBindings` path — is **superseded**: the real universal chokepoint is `execute_steps` (63 call sites vs `run_layer_program`'s ~4 direct, 9 incl. `_ep`/`_mesh`). The new spine is **`execute_steps(mesh, gpus, …)`**. Sections §0a/§0b(crate)/§4(loader)/§5/§6 and the mesh-tree/manifest/safety design **remain valid**; §1, §3's SuperOp framing, and the phase table are re-sequenced in `## PIVOT`. Everything below the PIVOT section is the original design record, kept for provenance.

## IMPLEMENTATION STATUS (2026-07-05, branch `feature/device-mesh`, 26 commits, all green)

**The entire pure-logic / declarative / planning layer is DONE + unit-tested; two hardware-touching pieces are GPU-validated; zero workspace regression.** Handover for the next session: `.agent-progress/device-mesh-HANDOVER.md`. Detailed commit map: `.agent-progress/device-mesh-status.md`. **(Status below predates the 2026-07-06 pivot; the placement/loader/safety half stands, the executor half is re-homed onto `execute_steps` — see `## PIVOT`.)**

- ✅ **Phase 0** — `hipfire-hardware` leaf crate (breaks dispatch→runtime cycle); `group:&[usize]` on collectives. **Coherence-gate GPU-validated.**
- ◐ **Phase 0b** — DeviceMesh (full accessors: coords/`group_along`/`stage_for_layer`/`band_xfer`/`CollectiveHint`); EP executor mesh-driven (**`ep_decode_parity` anchor PASS on 35B-A3B**); `resolve_mesh`. *Remaining (REINSTATED 2026-07-06):* the single+EP signature merge into ONE `run_layer_program(mesh, gpus, bindings: &mut [B], program)` — plan §1's north star. The earlier "NOT pursued (N1-shape)" decision is **RETRACTED** (drifted from §1); the union-struct concern is dissolved (ctx built inside; partials/residual_dim behind `ForwardBindings`; `run_moe` vs `run_moe_ep` = internal `mesh.has_axis(Ep)` branch). See phase0.md "DECISION (RETRACTED)".
- ✅ **Phase 1a** — `CollectiveHint` + `collective_for_policy` (mini-partitioner) + `layer_collectives`. *Remaining:* wiring hints into the live executor (needs the executor-integration, Phase-1b GPU work).
- ✅ **Phase 2 (pure layer)** — manifest types (`ShardPolicy`/`WeightEntry`/`StateEntry`/`FusedQkvLayout`/`PinTarget`, all variants tested); `Architecture::{weight_manifest,state_manifest}` seams; `placement_devices`, `validate_manifest`, `plan_manifest` (full compile step); production manifests for **llama, qwen2, minimax(MoE), toy**.
- ◐ **Phase 2 (GPU exec — `fulfill_manifest`)** — `weight_store.rs`: `WeightStore`/`WeightHandle`/`fulfill_manifest`. **Whole-tensor path (single + all PP + Replicate/Pin/Tied) AND `ExpertSharded` (EP compact-blob per rank) GPU-validated on gfx1151** (`fulfill_manifest_probe`: single-1×1 + emulated PP-2 + emulated EP-2; placement + `memcpy_dtoh` byte-oracle; the oracle caught the missing-`layer` key bug). Additive — no forward rewiring (that's Phase 3). Takes a `source(entry)->bytes` closure (arch owns on-disk naming); the EP path is a generic expert-outermost host gather (arch's forward owns the per-expert ptr-table + zeroed-dummy). **§6 transactional guard done** — mid-load failure frees already-uploaded buffers (`free_all`) and returns `Err`, never a half-loaded leaking mesh. **qwen35 `state_manifest` done** (hybrid Kv+Recurrent+Conv keyed by `layer_types` — first Recurrent+Conv consumer). **deepseek4 `weight_manifest` + `state_manifest` done** (MLA Q/O-LoRA + joint MQA replicated, routed experts `ExpertSharded`, `num_hash_layers` gate-bias split; validates `ExpertSharded` on its origin arch; conditional compressor/indexer/HC/MTP scoped out — all `Replicate`, file-shaped).
- ◐ **Phase 3 START** — the `source(entry)` closure now returns `(bytes, DType)` (real on-disk quant type → store tensor is forward-ready, not `Raw`). `examples/llama_store_load.rs` loads a REAL `qwen3-0.6b-llama.mq4`'s quantized projections through generic `fulfill_manifest` + a llama HFQ-backed source and byte+dtype-matches bespoke `Llama::load_weights` — **196 tensors / 28 layers, byte+dtype identical (MQ4G256), GPU-validated**. **WHOLE-MODEL store→forward done (bit-exact)** — llama `weight_manifest` gained q_norm/k_norm (qwen3, gated on `has_qk_norm`); a universal `source` mirrors the HFQ loader rule (F16/F32→F32, else raw+real dtype) for norms/embed/lm_head/projections; `llama_store_load` fulfills the FULL manifest, assembles a complete `LlamaWeights` from the store, and the forward is **logit-IDENTICAL to bespoke (max |Δ|=0, 311 tensors, gfx1151)** — a drop-in bit-exact replacement for the bespoke llama loader (single-GPU). **PP-2 store PLACEMENT done** — `output_norm`→`Pin(Output)` fix; `llama_store_pp` fulfills the whole manifest across 2 emulated stages, asserts mesh-correct banding (311 tensors 155/156, embed→0 / output_norm+lm_head→1 / layers by `stage_for_layer`), and the gathered forward is **logit-IDENTICAL to bespoke (max |Δ|=0, gfx1151)**.
- ✅ **Phase 1c (PP-2 banded EXECUTION)** — refactored llama's forward into `forward_scratch_band(layer_range)` + `forward_scratch_head` (`forward_scratch_compute` = band(0..n)+head, bit-exact). `llama_store_pp` runs a REAL banded PP forward — stage0 embed+band(0..14) on dev0 → `boundary_copy` → stage1 band(14..28)+head on dev1, **logit-IDENTICAL to bespoke (max |Δ|=0)**. Full pipeline-parallel LOAD + EXECUTE on a real model. *Remaining:* `ModelParallel`/`ArchDispatch` daemon hoist (serve-reach); real 2-GPU HW. *Remaining (Phase 2):* dense-TP slice (`Err`, Phase 5); qwen35 *weight* manifest (DeltaNet — loader study).

**REMAINING = the GPU-execution half, each its own coherence/oracle-gated PR (multi-session):** Phase 1b (PP executor loop + `BandXfer` wiring + PP byte-exact oracle) · 1c (llama-PP walking skeleton) · 2-GPU (`fulfill_manifest` upload) · 3 (`WeightStore`/`StateStore` + `ModelParallel` + `ArchDispatch` — hoist `EpArch`/`LoadedModel` out of the daemon example binary) · 4 (qwen2 `ForwardBindings` reach) · 5/5a/5b (live head-axis TP + DeltaNet head-shard + `s_ef_residual`; emulation heterogeneity `HIPFIRE_EMULATE_ARCHS/_VRAM`; ragged/mixed-arch per-arch `LoweredForward`) · 6 (Tier-2 slot-binding, optional) · 7 (initiate spec-decode/VL/TP follow-up tracks).

**Base note:** branched off `feature/parallel-expansion` (has `HIPFIRE_EMULATE_GPUS`, which Phase-1b/5a validation uses). `fulfill_manifest` is the natural next unit — it is now just the GPU execution of `plan_manifest`.

---

## PIVOT (2026-07-06) — the executor is `execute_steps(mesh, gpus)`, and it is the ONLY spine

### What the master merge revealed
The 519-commit master merge (`5b95cbd3`) invalidated the plan's executor assumption. A 4-agent read-only sweep of the post-merge worktree established the ground truth:

- Master shipped **`dense_forward`** (`crates/hipfire-runtime/src/arch_spec.rs:131`, commit `2a41f98f`): a shared dense-decode driver that builds a transient `Vec<Step>` per layer and feeds **`execute_steps(gpu: &mut Gpu, ctx, steps)`** (`crates/hipfire-dispatch/src/pipeline/steps.rs:600`). **llama** (`llama.rs:3460`) and **qwen2** (`qwen2.rs:1927`) now route through it. The merge **reverted `3a3c60e5`** (which had wired qwen2 → `run_layer_program_mesh`).
- There are now **two independent decode spines with no shared code** — parallel op vocabularies:

  | Spine | Op IR | Driver | Arches | Call sites |
  |---|---|---|---|---|
  | **A — dense** | `Step` | `dense_forward` → `execute_steps(&mut Gpu)` | llama, qwen2 (+ qwen35 & cohere2moe call `execute_steps` directly) | **63** |
  | **B — superop** | `SuperOp` | `run_layer_program[_mesh]` via `ForwardBindings` | deepseek4, minimax, lfm2moe | **~4** (9 w/ `_ep`/`_mesh`) |

- The device-mesh work built its whole executor (`run_layer_program_mesh`, `superop.rs:457`) on **Spine B**. Master made **Spine A** the dense default. `execute_steps` is the true chokepoint — even Spine B's prefill and `run_layer_program` bottom out in it. **The mesh belongs at `execute_steps`.**

### The new thesis (supersedes §1)
> **Exactly ONE executor: `execute_steps(mesh: &DeviceMesh, gpus: &mut Gpus, ctx, steps)`.** Replace the `gpu: &mut Gpu` parameter with `(mesh, gpus)` and execute the fan-out transparently inside. The `Step` IR is the single lowering for **all arches and all axes**; the `SuperOp`/`ForwardBindings`/`run_layer_program*` substrate is **retired for parallelism** and its one novel piece (the EP-MoE all-reduce lifecycle) is migrated into a `Step`.

**Decisions locked (2026-07-06):**
- **Grand-unify** — MoE/EP folds into `execute_steps` too. The "live" EP path (`run_layer_program_mesh` EP branch serving deepseek4/minimax) is **this branch's own phase-2 feature work**, not a production contract — free to refactor/retire. One spine, no exceptions.

> **CLARIFICATION (2026-07-07, bjoern) — "one spine / `Step::Moe`" means DECOMPOSE, not a monolithic op.** The grand-unify's intent is to **compose MoE/DeltaNet out of SEPARATE fine-grained Steps** (MoE = `Route` → grouped expert GEMM → `SiluMul` → grouped-down + combine; DeltaNet = projection Gemvs + `Step::Recurrent`/`Conv`), splitting WHAT (ops+operands) from HOW/WHERE (placement via the manifest, collectives via the executor) — exactly as `Step::Attend` already does for attention. A single `Step::Moe` that "absorbs `run_moe_ep`" would itself VIOLATE the split (it hides how/where inside a what-op); so would a primitive whose arch closure both runs the experts and knows its rank. **P-D shipped that primitive (`Gpus::ep_moe_allreduce`) as a byte-identical shortcut to delete SuperOp — treat it as DEFERRED decomposition / an interim, not the end-state.** The MoE sub-pipeline kernels already exist (`deepseek4_moe_topk_bias_aware`, `gemm_*_moe_grouped_*`, `moe_down_combine_grouped`), so this is a WRAP + refactor, not greenfield. Detail: `.agent-memory/notes/moe-deltanet-decompose-into-steps.md`.
- **Big-bang signature change** — flip `execute_steps(&mut Gpu → mesh, gpus)` across all 63 sites at once (every caller passes `DeviceMesh::single()` + the 1-device `Gpus`), then add the sharding. Same for the forward drivers that hold a `&mut Gpu` (`dense_forward`, qwen35 `forward_from_x_gpu`, cohere2moe `decode_step_body`, `prefill_forward` at `crates/hipfire-runtime/src/llama.rs:1498`). **Path note:** llama lives in **`crates/hipfire-runtime/src/llama.rs`**, not a `hipfire-arch-llama` crate — CLAUDE.md's "one crate per family" blurb misleads here; the bare `llama.rs:NNNN` refs below all mean `crates/hipfire-runtime/src/llama.rs`.

### The three axes decompose to three homes (the original plan conflated them — that was the error)
- **PP (inter-layer)** → lives **above** `execute_steps`, at the driver level. This is exactly what `forward_scratch_band(layer_range)` + `Gpus::boundary_copy` already do, **bit-exact** (`llama.rs:4205`, validated `max|Δ|=0`). Generalize that pattern into `dense_forward`; `mesh.stage_for_layer(L)` picks each stage's layer range. **KEEP.**
- **TP (intra-op)** → lives **inside** `execute_steps`. A `Gemv`/`GemvResidual`/`Attend`/`FusedQkv` step shards across the `Tp` group and all-reduces. **The manifest `ShardPolicy` (Column/Row/HeadSharded/FusedQKV) is exactly the per-Step shard rule** — single source of truth, already built. This is the "replace `gpu` with the mesh" core. **NEW.**
- **EP (intra-MoE)** → add **`Step::Moe`** (absorb `run_moe_ep`/`ep_add_into_residual` + the `Ep`-group all-reduce), so MoE expresses in the `Step` IR and `execute_steps(mesh)` does the expert-shard + reduce over the `Ep` group. Completes the unification; retires `run_layer_program_mesh`/`ep.rs`. DeltaNet needs **`Step::Recurrent`/`Step::Conv`** too for qwen35 to join the one spine. **REWORK (novel logic migrates) + NEW.**

### Keep / rework / orphan (from the built-pieces audit)
- **KEEP (spine-agnostic, all validated):** `hipfire-hardware` (`DeviceMesh`/`Gpus`/collectives/`boundary_copy`, `lib.rs`), `weight_manifest`/`plan_manifest`, `fulfill_manifest`/`WeightStore` (`weight_store.rs`), `tp_shard` (`ShardConfig`/head-ranges), `forward_scratch_band` (the PP driver pattern). These feed `execute_steps`'s per-rank `Gpu` fields — unchanged direction.
- **REWORK → `Step::Moe` dispatch:** `run_moe_ep`/`ep_add_into_residual` + EP all-reduce lifecycle (`superop.rs:352-533`). The one genuinely novel parallelism kernel; migrate it, don't lose it.
- **ORPHAN (retired by the pivot):** `run_layer_program_mesh` as a top-level executor (its 1×1 arm is already dead post-revert), the `run_layer_program_ep` shim (`ep.rs`), and reliance on `ForwardBindings`/`LayerProgram`/`SuperOp` for parallelism. *(The `SuperOp` substrate also underpins the pre-existing `HIPFIRE_FORWARD_LOWERED` single-GPU experiment; deleting it outright is a separate call — the pivot just stops routing parallelism through it.)*

### Why the 1×1 degeneration is free and byte-identical (the safety anchor)
`execute_steps(DeviceMesh::single(), gpus)` → dispatch straight to `gpus.devices[0]`, exactly as today. Proven mechanics: `Gpus::single` (`lib.rs:195`) moves the `Gpu` in verbatim (`active_stream: None`); the single-mesh path must **never** call `ensure_rank_streams` (`superop.rs:437`, the `None→Some` flip that silently switches hot-path memset sync→async — CLAUDE.md trap); `group_along(kind, [])` returns the singleton `vec![0]` and `all_reduce_sum_f32*` short-circuit `Ok(())` at `len()==1`. Net: zero collectives, zero stream change, **output byte-identical to pre-mesh** (already demonstrated on qwen2, md5 LOWERED=0==1, coherence 11/11 at `3a3c60e5` before the revert).

### Re-sequenced phases (supersede the phase table below)
Loader/placement half (Phases 0–2 manifest/`fulfill_manifest`) is DONE and unchanged. The executor half re-sequences to:

| Phase | Content | Validation |
|---|---|---|
| **P-A** *(mechanical, byte-identical)* | Big-bang flip `execute_steps(&mut Gpu) → execute_steps(mesh: &DeviceMesh, gpus: &mut Gpus, …)` across all 63 sites + the forward drivers (`dense_forward`, qwen35 `forward_from_x_gpu`, cohere2moe `decode_step_body`, `prefill_forward`) that hold `&mut Gpu`. Every caller passes `DeviceMesh::single()`. Internally degenerate to `gpus.devices[0]`, **no `ensure_rank_streams`**. Threads the mesh to the chokepoint, zero behavior change. | `HIPFIRE_FORWARD_LOWERED`-style committed-token md5 A/B == today, per arch; `coherence-gate.sh` |
| **P-B** *(TP inside `execute_steps`)* | Per-`Step` shard rule keyed by manifest `ShardPolicy`: `Gemv` Column/Row + all-reduce over `Tp` group; `Attend`/`FusedQkv` head-shard (`tp_shard` `q_head_range`/`kv_head_range`). `single()` = identity. First-ever live row/col TP. | `HIPFIRE_EMULATE_GPUS=2` TP + real 2-GPU; TP=1-vs-TP=2 logit parity (FP32, `HIPFIRE_DETERMINISTIC=1`) |
| **P-C** *(PP at the driver)* | Generalize `forward_scratch_band` + `boundary_copy` into `dense_forward`; `mesh.stage_for_layer` drives per-stage layer ranges; band-boundary residual copy. (llama already bit-exact; generalize + serve-reach.) | PP oracle (hand-band `=0` vs generic `=1`) + coherence, emulated + HW |
| **P-D** *(EP/MoE into the `Step` IR)* | Add `Step::Moe` (absorb `run_moe_ep`/`ep_add_into_residual` + `Ep` all-reduce); migrate deepseek4/minimax/lfm2moe/qwen35-MoE FFN from `SuperOp` onto `Step`; retire `run_layer_program_mesh`/`ep.rs`. | `serve-multiturn-gate.sh` green for all MoE arches; EP emulated + HW |
| **P-E** *(complete the unification)* | `Step::Recurrent`/`Step::Conv` for qwen35 DeltaNet → its bespoke forward joins `execute_steps`; DeltaNet head-shard (`HeadSharded` + co-sharded S-matrix/conv state + wire `s_ef_residual`, qwen35.rs:1091). Then compose (5a/5b heterogeneity/ragged/mixed-arch) unchanged from the original plan. | DeltaNet TP coherence + PP determinism oracle |

`ModelParallel`/`ArchDispatch` daemon rehome (old §5 / Phase 3) and the composed/ragged/mixed-arch work (5a/5b) carry forward unchanged — they sit *above* the executor and don't care whether it's `Step`- or `SuperOp`-based.

---

## Context

hipfire's three multi-GPU strategies are scattered, per-arch, and non-transparent:

- **PP (pipeline)** is hand-coded in **one** arch (qwen35): decode `forward_scratch_layers_multi` and prefill `forward_prefill_batch_multi` hand-loop layers and `gpus.boundary_copy` the residual at each layer-band boundary. (Verified serve-wired end-to-end this session: `HIPFIRE_EMULATE_GPUS=2 HIPFIRE_PP=2` via the Bun CLI → coherent qwen3.5-4b output.)
- **EP (expert-parallel)** has a generic *decode* executor — `hipfire_runtime::ep::run_layer_program_ep` drives a `LayerProgram` across ranks, special-casing `SuperOpKind::Moe → all-reduce`. deepseek4 + minimax + qwen35-MoE use it. But **prefill EP** all-reduce is hand-inlined per arch (`forward_prefill_batch_ep`), *not* through `ep.rs`.
- **TP (tensor-parallel)** sharding math exists (`ShardConfig`), but drives **no live forward**; today `tp>1` in the daemon *means EP*. Real row/column TP is greenfield.
- **Loading is per-arch + imperative**: sharded weight upload is bespoke inherent methods, *not* on the `Architecture` trait; routing is **tp/pp-param-driven** (`tp>1 → load_model_ep`, `pp>1 → load_model_pp`) with `arch_id` as *internal guards* (`load_model_ep` accepts only 9/10; `load_model_pp` only 5/6 at daemon.rs:4843). Four near-duplicate `load_model_*` end in a `LoadedModel{…}` literal (**52 fields / 36 `Option`**). PP state (`pp_gpus`) and EP state (`EpState`/`EpArch`, only `Ds4|Minimax`) are **separate ownership universes** in the daemon *example binary* — the documented EP-rehome blocker.
- **State placement is imperative + format-exploded**: ~15 `KvCache::*_multi` ctors funnel into 3 hand loops; DeltaNet keeps its own `la_to_device` sidecar (LA-compact index ≠ KvCache global index); lfm2moe conv-state has *no* multi path.

**Goal:** any model implementing `ForwardBindings` + declaring its `LayerProgram` (and a weight/state manifest) gets PP/TP/EP **for free** — placement, sharding, and cross-device sync driven entirely by the engine. **Ambition: maximal transparency-by-construction** — the model's *forward path* is structurally unable to observe parallelism (device count, rank, global-shard dims, layer index, or collective calls never enter forward-time arch code; wrong parallelism code fails to compile). Reached via a **phased, no-regression roadmap**; each phase is its own coherence/byte-exact-gated PR.

**Two tiers of transparency (name them explicitly — do not conflate).**
- **Tier 1 — device/dim transparency (Phases 0–5, shippable):** the `DeviceMesh` drives compute *and* placement, so the arch never observes device count, rank, or global sharded dims. Weights/state are sliced to `(stage,tp_rank)` before the arch receives them; the arch keeps resolving operands from its own (now-local) fields. This is real and deletes the PP/EP/loader duplication.
- **Tier 2 — layout/operand transparency (Phase 6, *optional* stretch — likely not worth the full migration):** the executor would hand the arch each operand *per-op by slot* (the `superop.rs` slot-binding seam), so the arch couldn't inspect its local layout. Greenfield assessment: this chases GSPMD-grade structural unobservability, which is a *property of having an array compiler* — hipfire deliberately has none (hand HIP kernels, `KernelKey` frozen at lower time). Over a **curated** arch set, "arch reads its own pre-sliced local fields" (Tier 1) is a defensible **permanent** stopping point; a slot migration across all 5 arches buys little. **Downgraded from "the finish line" to an open question.** Non-blocking regardless — Phase 2/3 must never depend on it (the slot seam is verifiably empty: `ScratchSlot` has zero constructions today).

**Tier-1 boundary — head-axis sharding makes FusedQKV *and* DeltaNet transparent (they are NOT `Custom`).** Both shard along the **head axis**, not row/col, and the math already exists in `tp_shard` as *dead scaffolding* — this design is its first consumer:
- **`FusedQKV{q_heads,kv_heads,head_dim}` + a declarative fused-block layout descriptor** (where the Q|K|V|gate boundaries sit — DeltaNet `q|k|v`+separate `wz`, FullAttn `q|gate`, vanilla `q|k|v`): the engine splits the fused tensor at the boundaries, shards each sub-block by its head group via `q_head_range`/`kv_head_range`. Layout is *data*, not code.
- **`HeadSharded` for DeltaNet `w_alpha/w_beta/wz`**: each rank owns a subset of linear-attention heads (`dn_value_head_range`); its recurrent S-matrix/conv state co-shards by head (a `StateEntry` head-sharding); per-head recurrence is independent; cross-head mix is `wo` row-shard + all-reduce (standard TP). Requires wiring the currently-dropped `s_ef_residual` normalization (qwen35.rs:1091) for correctness.

Both are **load-time architectural declaration** (head structure), not forward-time parallelism observation → still Tier-1-transparent. **Greenfield caveat:** the `tp_shard` head-range math has never driven a forward — this is real Phase-5 work, but declarative, not an escape hatch. So `Replicate/Column/Row/FusedQKV/HeadSharded/Vocab` are all generic; **only `ExpertSharded`** carries an irreducibly arch-specific convention (MoE packed-blob + pointer-table + zeroed-dummy) — a rich policy with a per-arch descriptor. `Custom{shard_fn}` shrinks to "genuinely bespoke, no known example in the current fleet."

The substrate is ~60% built: `LayerProgram`/`SuperOpKind`/`ForwardBindings` exist (`crates/hipfire-dispatch/src/pipeline/superop.rs`) and **5 of 8 arches drive their (default-on) lowered single-GPU forward through it** (qwen35, qwen2, deepseek4, minimax, lfm2moe). This design finishes and generalizes that seam.

---

## North-star architecture (target end-state)

### 0a. Placement is a **`DeviceMesh` tree of typed `Dimension` nodes** — PP / TP / EP compose, and topologies may be ragged
**Primary model = a named-axis mesh** (industry-converged: JAX `Mesh(('pp','tp','ep'))`, PyTorch `DeviceMesh`, Megatron process-groups): a device's **coordinate** is an index tuple over the present axes `{Pp, Tp, Ep}`, and its **collective group along an axis** = all devices sharing the *other* coordinates. This already expresses **coexisting TP and EP at different group sizes** — a MoE model reduces its expert op over the `Ep` group and its attention over the `Tp` group, exactly how Megatron/JAX do it (`group_along(kind, coord)` + `CollectiveHint{kind}` is the mechanism; *named axes*, not nesting, are what's required).

**The mesh is stored as a `Dimension` tree solely to allow raggedness** — a named-axis mesh is strictly *rectangular* (uniform `Tp`/`Ep` size across all `Pp` stages), which **heterogeneous / mixed-arch hardware can't live within**. The dev fleet is deliberately mixed (hipx = gfx1151 + gfx1010; contributor rigs pair APU+dGPU or 7900XTX+5700XT), so **different shard-group size per stage** is a real need — TP-2 on the strong card's stage, TP-1 on the weak card's — which a rectangular mesh forbids and a tree (each `Pp` node carries its own sub-tree) allows. Ragged *pipeline band sizes* already exist (`init_layers`/`HIPFIRE_PP_LAYERS`); the tree extends the same raggedness to `Tp`/`Ep`, and `init_vram_weighted` (multi_gpu.rs:147 stub) chooses the shape from per-device capability. So: **named-axis semantics primary; tree representation only for raggedness** — the common rectangular case is `DeviceMesh::rect([{Pp,n},{Tp,n},{Ep,n}])` and never touches the tree.

**Mixed-arch meshes need per-arch *lowering*, not per-device caps.** `ArchCaps` + `FeatureFlags` are **already per-device** — they live on `Gpu` (dispatch.rs:332-334), each built from that device's detected arch (`FeatureFlags::from_env(&gpu.arch)`); nothing to restructure. The real gap: `KernelKey`s are resolved and **frozen at lower time** (superop.rs:14) via arch-conditional selection (WMMA on gfx1151 vs not on gfx1010), so a single `LoweredForward` can't run on two arches. On a mixed-arch mesh, **`LoweredForward` becomes keyed by arch** — lower once per distinct arch present, and each mesh cell runs the lowering matching its `arch_caps`. Supporting items (Phase 5b): promote the mixed-arch preflight from a default *refusal* (multi_gpu.rs:731-750, `HIPFIRE_ALLOW_MIXED_ARCH` override) to a first-class path; audit `FeatureFlags` for any kernel gate that doesn't check the *executing device's* `arch_caps`.

**Discipline (so the tree isn't the N1 over-build):** the **rectangular case is a one-line convenience constructor** `DeviceMesh::rect([{Pp,n},{Tp,n},{Ep,n}])`, so the common path never touches raggedness; the tree is contained in `hipfire-hardware` and `group_along(kind, coord) -> &[usize]` returns a flat device-id slice, so **the executor + collective signatures are identical whether the mesh is rectangular or ragged** — adopting the tree ripples nowhere above hardware. Degenerate cases: single-GPU = one leaf; PP-only = root with N leaves; EP-only = today's `init_tp`. No `EpPp` special mode; the `pp>1 && tp>1` refusal lifts in Phase 5b once the multi-axis loader/dispatch exists.

Collectives become **group-scoped and level-named**: `AllReduce{kind: Tp|Ep, …}` reduces over the device's group *along that `Dimension`*; `BandXfer` copies between adjacent `Pp` siblings. This needs a real (small) change, NOT free: today `all_reduce_sum_f32[_peer]` **reject** a buffer subset (`buffers.len() != devices.len()`) and index `devices`/`peer_ar_tmp`/streams *positionally over all devices* (multi_gpu.rs:492/604). Add a `group: &[usize]` (global device ids) param to both, indexing through `group[k]`, in **Phase 0's mechanical move** — current call sites pass `group = 0..n`, keeping every 1D case byte-identical; then Phase 5b is pure wiring, not a signature break. The RCCL path additionally needs per-group communicators (`ncclCommSplit`), deferred to 5b.

The daemon `pp>1 && tp>1` refusal (daemon.rs:1861) is **lifted only in Phase 5b**, once the 2D loader + dispatch exist — Phase 0's mesh relabel *keeps* the refusal (lifting it earlier would silently route a 2×2 request into 1×N EP, dropping `pp`).

**Discipline (avoid the N1 over-generalization bounce):** adopt the tree as the *data model* early (a relabel of `layer_to_device`/`band_starts` + the `group_along(kind, coord)` accessor in `hipfire-hardware`, `rect(...)` constructor for the common case — near-zero cost, single-axis behavior unchanged), but only *exercise* a multi-axis / ragged forward in the late composition phase. Real consumers: **hiptrx (4× gfx1201)** → 2×2 PP×TP; **hipx (gfx1151 + gfx1010)** → ragged (asymmetric stage sizes); `HIPFIRE_EMULATE_GPUS` fakes a 2×2 mesh on one card for dev — not speculative.

### 0b. Extract `hipfire-hardware` so ONE executor can live in ONE crate
The naive split (single-GPU `run_layer_program` in dispatch, parallel executor in runtime) concedes the "one executor" premise — two `run_*` in two crates — because the EP/PP arms need `Gpus`/collectives, which sit in runtime, and dispatch→runtime would cycle. **Fix the crate graph instead of the executor:** extract `multi_gpu.rs` (`Gpus` + `boundary_copy`/`all_reduce`/`enable_peer_all`/`init_*`/device-resolution) into a new **leaf crate `hipfire-hardware`** (deps: `hip-bridge` + `rdna-compute` only — RCCL is already a leaf in `hip-bridge`). The *only* coupling to break is 5 `crate::config::get()` reads in `multi_gpu.rs` (`tp_use_rccl`, `devices`, `emulate_gpus`, `allow_mixed_arch`, `uniform_vram_tolerance_gb`) — pass them in as an explicit `DeviceResolveOpts` param, which also removes a hidden global from the hardware layer. Then **`hipfire-dispatch` depends on `hipfire-hardware`** and hosts the whole executor.

### 1. Exactly ONE executor — `run_layer_program(mesh, gpus, lowered, bindings)`
> **SUPERSEDED by `## PIVOT` (2026-07-06):** the one executor is `execute_steps(mesh, gpus)` on the `Step` IR, not `run_layer_program` on `SuperOp`. The 1×1-identity and collective-hint reasoning below is correct and carries over verbatim to `execute_steps`; only the IR/function name changes. Read for the mesh-degeneration argument, not the entry point.

There is **no `ParallelMode` enum and no `run_layer_program_parallel`**. Single-GPU is a **1×1 `DeviceMesh`**: a singleton TP-group (all-reduce over one buffer = identity) and one stage (BandXfer never fires), so the *same* executor runs it with all collective hints `None` — a per-op branch that is not taken, byte-identical to today's single-GPU output. PP is N×1, EP/TP is 1×N, stacked is N×M — all the *same* function; the mesh is the only thing that changes. The executor lives in **hipfire-dispatch** (enabled by the Phase-0 `hipfire-hardware` extraction), is generic over `B: ForwardBindings`, and takes `&mut Gpus` + the mesh from hardware. **The arch is transparent to device count** (Tier 1) — it declares a `LayerProgram` + implements `ForwardBindings`, and calls one executor; whether it runs on 1 GPU or a 2×2 mesh is never visible to it. The 1×1 arm **must leave `devices[0]`'s stream state untouched** — it never calls the EP-only `ensure_rank_streams` (which flips `active_stream None→Some` and silently switches the hot-path memset sync→async, per CLAUDE.md); only the ≥1×N arms touch streams. (`Gpus::single` — multi_gpu.rs:156 — already models the 1-device set with no RCCL/peer state; it consumes the `Gpu` by value, so the daemon owns its `Gpu` inside a `Gpus` for the process lifetime — a mechanical Phase-0b refactor, zero runtime cost.)

Sync lives at two scopes because it genuinely does in the code:
- **Per-op** `CollectiveHint::AllReduce{kind: Tp|Ep, rows*dim}` — `Ep` for `Moe` ops, `Tp` for row-sharded `o_proj`/dense. Executor issues `gpus.all_reduce_sum_f32[_peer](group_along(kind, coord), …)`. **Derived from the `ShardPolicy`, not hand-injected** (greenfield: single source of truth). A `RowShard`/`ExpertSharded` weight *mechanically implies* an all-reduce on its consuming op — a **transformer-template mini-partitioner** in generic lowering reads the manifest policy and emits the hint, so sharding is declared **once** (in the manifest) and the collective can't be forgotten (kills the silent-wrong-reduce class). This runs as a post-pass over each arch's `*_lower_program` output (where `KernelKey`s are frozen), needing no per-arch collective code.
- **Per-layer-boundary** `BandXfer{src_dev,dst_dev}` on `LoweredForward.band_xfer: Vec<Option<BandXfer>>` indexed by layer — PP. Executed by the **outer driver** *between* per-layer `run_layer_program` calls (PP sync fires between layers; a per-op hint cannot see the previous layer's device). A 1×1 or 1×N mesh has all-`None` band_xfer and all-`None` op hints → hot path byte-identical.

### 2. IR carries `n_rows` — prefill and decode unified (and it must be an *arbitrary token set*, not contiguous prefill)
The current IR is **decode-only** (per-token; `superop.rs` has no batch dim; `ep.rs` reduces a scalar `residual_dim`). Prefill is 3 bespoke drivers (single-GPU batched, PP `forward_prefill_batch_multi` with N-row band handoff, EP `forward_prefill_batch_ep` with hand-inlined `n*dim` all-reduce). Generalize the executor to carry `n_rows`: `BandXfer` copies `n_rows*dim`, `AllReduce` reduces `n_rows*dim`. This deletes all three hand-rolled loops, not one.
- **Bake-in decision (cheap now, expensive later):** define `n_rows` as an **arbitrary position-indexed token set**, NOT prefill-contiguous, and verify the EP/TP all-reduce + BandXfer never assume row contiguity. The DFlash/DDTree spec-decode stack (a Phase-7 track) is **tree-structured** (draft + tree-mask verify + per-node acceptance); if Phase 2 bakes `n_rows`=contiguous batch, the "drafter as mesh coordinate" track inherits an IR that can't express cross-rank tree-verify and pays twice. This is the single Phase-0–5 decision that could foreclose a Phase-7 track — reserve against it in Phase 1a/2.

### 3. Collectives owned by the executor, not the arch
**Tier 1 (now):** the EP pattern already ships — `ForwardBindings::run_moe_ep` writes a partial into an arch-owned tensor and `ep_add_into_residual` reads it back post-reduce; the executor issues the collective. Generalizing the `CollectiveHint` path keeps this shape: the arch op-body produces the partial in its own scratch, the executor reduces it. No arch op-body *calls* a collective — enforced by return-variant convention + review.
**Tier 2 (Phase 6):** `dispatch_super_op` returns `OpOutput::{Complete, Partial(ScratchSlot)}` and the executor resolves `ForwardBindings::resolve_scratch(slot) -> &GpuTensor` — but `ScratchSlot` has **zero constructions today** and `resolve_scratch` doesn't exist (the slot-binding seam is empty TODO, superop.rs:244-247), so this is the same Tier-2 migration as layout-transparency, not a Phase-1 mechanism. `ScratchSlot` is a `Copy` enum (`Activation(u32)|State(u32)|Cache(u32)`, superop.rs:55) → no lifetime conflict when it does land. **The compile-time guarantee** (arch physically can't `use …::all_reduce`) rests on two things: (a) the `hipfire-hardware` leaf crate from **Phase 0** — arch crates never list it as a direct dep, so its collective symbols are unnameable there; and (b) **rehoming the per-arch driver loops** (`forward_ep`, `forward_scratch_layers_multi`, …) up into runtime so they, not the arch crates, hold `&mut Gpus` (Phase 6). Until (b) lands, arch crates still reach collectives via their existing `hipfire-runtime` dep, so the interim guarantee is "executor owns collectives, op-bodies don't call them" (≈90% of the value) enforced by return-variant + review; after Phase 6, wrong code fails to compile.

### 4. Loading is mesh-driven too — placement = manifest (what) × mesh (where)
**The loader takes the same `DeviceMesh` as the executor** (§1), so weights and state land on the correct `(stage, tp_rank)` locations transparently — the arch declares *what it needs* and never names a device, rank, or device count on the load side any more than on the forward side. Symmetry: the executor projects the `LayerProgram` onto the mesh for *compute*; `fulfill_manifest` projects the manifest onto the mesh for *placement*.

`Architecture` gains `fn weight_manifest(cfg) -> Vec<WeightEntry>` and `fn state_manifest(cfg) -> Vec<StateEntry>` (pure CPU, device-count-agnostic — **derived per load, never serialized into `.hfq`, so there is no on-disk back-compat surface**: manifest versioning is defined out of existence, do not add serialization):
- `WeightEntry{name,logical_shape,dtype,policy}`, `ShardPolicy { Replicate | ColumnShard{axis} | RowShard{axis} | ExpertSharded{n_experts,assign} | FusedQKV{q_heads,kv_heads,head_dim,layout} | HeadSharded{n_heads,head_dim} | Tied{source} | Pin{Embed|Output} | VocabShard{axis} | Custom{shard_fn} }`. `FusedQKV`/`HeadSharded` shard the **head axis** via `tp_shard`'s `q_head_range`/`kv_head_range`/`dn_value_head_range` (see Tier-1 boundary above); `FusedQKV.layout` is the declarative Q|K|V|gate block descriptor.
- `StateEntry{ kind: Kv{quant} | Recurrent | Conv, layer_scope }` — the state sibling; same mesh projection, collapsing the ~15 `KvCache::*_multi` ctors and the DeltaNet `la_to_device` sidecar into one keyed `StateStore` (global-layer vs LA-compact index reconciled **in the engine**).

**`fulfill_manifest(manifest, hfq, mesh)` is one loop** driven by the mesh accessors — `mesh.stage_for_layer(L)` (the `Pp` coordinate owning a layer) and `mesh.group_along(Tp|Ep, coord)` (the shard group for a sharded weight along its axis):
- **Where (PP axis):** a layer's weights/state go to the stage owning that layer; `Pin{Embed}` → stage 0, `Pin{Output}`/`VocabShard` → last stage (the Megatron output convention, now a mesh projection not a hardcode).
- **How, within the owning stage (TP axis):** `Replicate` → full tensor to every rank in the TP-group; `ColumnShard/RowShard{axis}` → `tp_size` slices along axis, slice `r` → rank `r`; `ExpertSharded` → experts distributed across the group (deepseek4 zeroed-dummy trick, **once, in the engine**); `FusedQKV` → `tp_shard`'s GQA-aware `q_head_range`/`kv_head_range`; `VocabShard` → the (currently nonexistent) TP logit cursor.
- **Transactional (partial-failure safe):** `fulfill_manifest` uploads real-dtype tensors, so a rank can OOM mid-loop (the Phase-1 shape-only dry-run uses *zeroed* descriptors and won't catch VRAM exhaustion). On any per-cell alloc failure, **free all already-populated cells and return `Err((coord, WeightEntry))`** — never leave a half-loaded mesh.
- **VocabShard changes the sampling path** (specify it, don't leave implicit): `generate_ep` gathers logits on rank 0 and samples there today, assuming a **replicated** lm_head. With a vocab-split lm_head, the shards must be **all-gathered to one designated rank**, after which the existing single-rank sampler / EOS filter / loop-guard / `sampler_overrides` blocked-token logic runs **unchanged**. (Distributed argmax+renorm is the alternative but needlessly complicates greedy+sampling+EOS; prefer gather.)
- Each tensor is **sliced to its `(stage,tp_rank)` before the arch receives it** → global dims never enter arch code.
- **The store is a load-side *placement* container, keyed by `(logical_name, mesh_coord)`** — it populates the arch's existing per-device weight/state fields (the same fields `load_weights_multi` fills today), replacing the god-struct's placement bookkeeping. Its value should be a **handle enum (`Resident(GpuTensor)` | future `Paged(WeightId)`)** so the deferred `WeightPager` × mesh axis slots in additively without re-keying. It does **NOT** feed the forward by slot: the executor still reads arch fields (Tier 1). Slot-keyed operand resolution `(layer,slot,mesh_coord)` is **Tier 2 / Phase 6 only** — adopting it now would invert the dependency onto the unbuilt `superop.rs` slot-binding seam (`OpBinding.weights/scratch` are empty TODO today).
- `StateStore` must be **keyed by global layer index**, not a compacted `Vec` — that is what defines the DeltaNet `la_to_device` sidecar out of existence (LA-vs-full-attn knowledge lives in *manifest construction*, which the arch owns via `config.layer_types`; `stage_for_layer(global)` needs none of it). Note the pre-existing multi-path `s_ef_residual` drop (qwen35.rs:1091) is a real single-vs-multi numerical divergence — bake it into the Phase-1b PP oracle's expectations.
- The **same manifest** fulfills on 1×1 (everything to the one device), N×1 (banded by stage, no slicing), 1×N (one stage, sliced across ranks), and N×M (both) — zero arch change across all four.
- **`WeightPager` × mesh is explicitly deferred/out-of-scope** (not a drop-in): the pager is inert single-device scaffolding (`WeightId` has no device axis; `free_gpu_multi`/`shard_moe_experts` currently *assert* `pager.is_none()`). UMA lowers its priority; revisit as its own item if >VRAM-across-mesh is needed.

### 5. The proper-abstraction endgame: ONE runtime object + an arch vtable
This is what makes the design *engine-level* at the daemon layer, not just the executor. **Scope reality:** `EpState`/`EpArch`/`LoadedModel` (52 fields / 36 `Option`) live in the daemon **example binary** (`examples/daemon.rs`), not a library — so Phase 3's real, least-mechanical, riskiest content is **hoisting this machinery into the runtime lib** as `ModelParallel` + `Box<dyn ArchDispatch>` (the "Phase 3 fallback: god-struct stays as compat" escape is example-local). Collapse the god-struct + every `arch_id==` guard into exactly:
`LoadedModel = { tokenizer, mesh: DeviceMesh, parallel: ModelParallel, arch: Box<dyn ArchDispatch> }`.
- `ModelParallel{ gpus, mesh, weights: WeightStore, state: StateStore }` absorbs `EpState`/`EpArch` + `pp_*` — **one** reset/checkpoint/abort site (the documented EP-rehome blocker for qwen35-hybrid, and the #462 state-bleed surface).
- `ArchDispatch` is the trait object the daemon calls instead of matching `arch_id`: `weight_manifest`/`state_manifest`/`lower_program`/`make_bindings` + sampler/eos/prompt overrides. The manifest declaration is its natural attachment point. This is what removes the `arch_id==` ladders scattered through the daemon and is the linchpin of daemon-side transparency.

### 6. Safety-by-construction (woven through phases)
- **Collective-manifest diff** `validate_rank_programs` (renamed from `validate_ep_manifest` — it now validates any mesh's per-rank sequence, TP/PP too): compare only the `SuperOpKind` sequence per rank (NOT `KernelKey`, which legitimately differs under TP); disagreement → refuse at init. CPU, CI-testable.
- **Shape-only dry run** at load (zeroed descriptors, no-op collectives) → TP shard-math shape bugs fail at load, not token-1 page fault.
- **Transactional `fulfill_manifest`** (see §4) — free-and-`Err` on any mid-load OOM; never a half-loaded mesh.
- **NaN-poison** the PP residual source after `wait_boundary` (gated `HIPFIRE_PP_NAN_POISON`/`debug_assertions`).
- **`(device_id, request_epoch)` stamp** in `ModelState`, checked atop advancing-state ops — covers **both `Recurrent` (DeltaNet) and `Conv` (lfm2moe)** — turning the #462 request-bleed class into an immediate panic.
- **Determinism invariant (protects the byte-exact gates):** all sampling RNG is drawn on **one designated rank after the logit gather** — no per-rank forward RNG. Corollary for the Phase-1b PP oracle: pin a fixed seed + assert identical draw count/order across the `=0`/`=1` arms (the DeltaNet `s_ef_residual`-drop **stochastic** path, qwen35.rs:1093, is otherwise not byte-reproducible — the oracle compares multi-vs-multi, where both arms drop EF and match; single-vs-multi divergence is separately accepted, then removed when Phase 5 wires `s_ef_residual`).
- **Observability:** `HIPFIRE_MESH_DUMP=1` / `mesh.describe()` at load prints the resolved tree + coord→(dev,rank) + per-layer stage/group placement — a wrong mesh should be a readable dump, not a page fault.

### Invariants
- **Single-thread HIP**: the executor stays single-threaded (device-switch + one async stream per rank; FIFO stream overlap). It must **never** spawn an OS thread per rank. The deferred async-overlap optimization must honor this.

---

## Phased roadmap (each phase = its own no-regression, gated PR)

> **SUPERSEDED for the executor half by `## PIVOT` phases P-A…P-E (2026-07-06).** Phases 0/0b/1a/1b below described building the `run_layer_program`/`SuperOp` mesh executor — that spine is retired. The loader/manifest content (Phase 2), safety (§6 lands), heterogeneity emulation (5a), and composed/ragged (5b) are unchanged and still apply. Cross-reference the PIVOT table for the current executor sequence.

| Phase | Content | Validation |
|---|---|---|
| **0** | **Extract `hipfire-hardware` leaf crate** (`Gpus` + collectives + device resolution, config→`DeviceResolveOpts` params); make `hipfire-dispatch` depend on it. **Adopt the `DeviceMesh` tree data model** (`Dimension{Pp\|Tp\|Ep, children}` + `group_along(kind, coord) -> &[usize]` + `rect(...)` ctor; flat layouts = degenerate trees), add the `group: &[usize]` param to collectives, and **replace `resolve_parallelism(pp,tp,…) -> (usize,usize)` with `resolve_mesh(...) -> DeviceMesh`** (the current resolver returns `(1,n)` only when `pp==1&&tp==1` — it structurally *cannot express a 2×2*, so the emulated mesh the roadmap depends on is otherwise unreachable). Pure mechanical move, single-axis behavior byte-unchanged. | `cargo build --workspace`; all gates unchanged (no logic touched) |
| **0b** | Unify the single-GPU `run_layer_program` **and** decode `ep.rs` into ONE mesh-driven `run_layer_program(mesh, …)` in hipfire-dispatch (single-GPU = 1×1 mesh, EP-decode = 1×N mesh). The executor takes `&mut [B]` (len = leaf count; `run_layer_program` is `&mut B`, `ep.rs` is `&mut [B]`) — single-GPU passes a **1-element slice** indexed `[0]`; mechanical call-site change across the 5 migrated arches, output byte-identical. Add `validate_rank_programs`. (Prefill EP untouched here — say so.) | `cargo test --lib`; byte-identical for BOTH single-GPU (`HIPFIRE_FORWARD_LOWERED` oracle) and EP-decode |
| **1a** | `CollectiveHint` (per-op) + `OpOutput` return-variant + **`n_rows` IR generalization**; migrate EP **decode+prefill** to hints. One coordinated cross-crate commit (≈4 impls × 8 methods = 32 signatures — must land atomically or the workspace won't build). | byte-identical EP via `HIPFIRE_FORWARD_LOWERED=0/1` md5 A/B |
| **1b** | qwen35 PP (decode `forward_scratch_layers_multi` + prefill `forward_prefill_batch_multi`) → `BandXfer` per-layer annotation; delete hand loops. **Build the PP oracle** (retain hand loop as `=0` ref, generic PP as `=1`, md5 A/B under `HIPFIRE_EMULATE_GPUS=2` + `pp=2`). | PP oracle + coherence gate |
| **1c** *(new — walking skeleton)* | `LlamaBindings: ForwardBindings` + llama PP via a **bespoke loader** (no manifest yet). Proves the generic executor generalizes to a second, architecturally-different arch (dense plain-attn vs qwen35 DeltaNet-hybrid) **before** the loader/state refactor. | llama PP coherent (emulated) |
| **2** | `weight_manifest` + `state_manifest` + **mesh-driven `fulfill_manifest(manifest, hfq, mesh)`** (placement = manifest × mesh, via `stage_for_layer`/`tp_group`); migrate ds4/minimax EP + qwen35/llama PP loaders to manifests; `Pin{Embed|Output}`/`VocabShard` as mesh projections; collapse `KvCache::*_multi` + `la_to_device` into `StateStore`. | Same manifest loads on 1×1/N×1/1×N; EP+PP coherence (emulated/HW) |
| **3** | `WeightStore`/`StateStore` keyed + unified `ModelParallel` (EP-rehome); collapse god-struct + `arch_id==` ladders → `ArchDispatch`. **Epoch stamp (Recurrent+Conv) lands here.** | `serve-multiturn-gate.sh` green for ALL EP arches (ds4/minimax/qwen35-MoE) |
| **4** | `qwen2` (+ cohere2moe when the crate lands) `ForwardBindings` + PP/EP reach — additive arch migrations (depend only on Phase 1+2). VL/dots-ocr vision **out of scope**. | per-arch coherence gate |
| **6** *(non-blocking quality)* | Structural hardening for the compile-time "unspeakable collectives" guarantee: the crate firewall already exists (Phase 0's `hipfire-hardware`), so this only **rehomes the per-arch driver loops** (`forward_ep`, `forward_scratch_layers_multi`, …) up into runtime and **drops the arch→hardware direct dep**, so arch crates physically can't `use` a collective. Plus withhold `layer_idx` (requires the `ScratchSlot` binding migration, superop.rs TODO). Sequenced **last, non-blocking**, so it can't repeat the N1 rejection by gating shipped value. | compile-time (arch crate can't name a collective); no behavior change |
| **5-dense** | Live row/col TP on a **dense** arch (llama): `ColumnShard`/`RowShard`/`VocabShard` + `FusedQKV` (activating `tp_shard`'s dead `q_head_range`/`kv_head_range`) → TP-group all-reduce hints; VocabShard gather+sample path (§4); divisibility-derived axis; disambiguate `tp>1` routing (EP vs true-TP). *First-ever live row/col TP forward.* | TP coherence incl. GQA (llama/qwen35 dense) |
| **5-deltanet** | DeltaNet head-shard (`HeadSharded`, activating dead `dn_value_head_range`) + **co-sharded recurrent S-matrix/conv state** + wire the dropped `s_ef_residual` (qwen35.rs:1091, the stochastic-path source). Highest-risk TP cell: dead math + dropped residual + non-determinism compound here. | qwen35 DeltaNet TP coherence + PP oracle determinism |
| **5a** *(prerequisite for 5b — heterogeneity emulation)* | Extend this session's `HIPFIRE_EMULATE_GPUS` harness so the split vGPUs can be **heterogeneous on one physical card**: `HIPFIRE_EMULATE_ARCHS=gfx1151,gfx1100` forces a per-vGPU arch (per-index generalization of the global `HIPFIRE_TARGET_ARCH`, dispatch.rs:494 → per-cell `ArchCaps`/`FeatureFlags`/lowering); `HIPFIRE_EMULATE_VRAM=48,16` forces per-vGPU reported memory budget (drives ragged / `init_vram_weighted` placement). **Define the coord→(physical_dev, logical_rank) lowering under aliasing:** `alias_ids` maps distinct ranks onto physical device 0 (`id.rem_euclid(real_count)`), but the mesh must keep aliased cells as **distinct logical ranks** (for state/stream identity) while suppressing peer-enable for same-physical-device pairs (multi_gpu.rs:263 already handles this). **Constraint:** the forced arch must be ISA-runnable on the physical card (gfx1100-on-gfx1151 ✓ downlevel-compatible; gfx1201-on-gfx1151 ✗). So emulation validates the mixed-arch *plumbing* (per-arch `LoweredForward`, mixed-arch preflight, VRAM-weighted placement) on the dev box; RDNA4-specific *codegen* still needs real hiptrx. | mixed-arch + ragged paths run on gfx1151 alone (no hiptrx/hipx needed) |
| **5b** | **Composed / ragged / mixed-arch mesh** (≥2 `Dimension`s): group-scoped all-reduce (`Tp`/`Ep` kind) + `BandXfer` across `Pp` siblings (falls out of Phases 1+5, no new mode); ragged sub-trees (per-stage TP/EP size) + `init_vram_weighted` policy; **per-arch `LoweredForward`** (lower per distinct arch; each cell runs its own) + promote mixed-arch preflight to first-class + arch-gate audit of `FeatureFlags`; RCCL per-group comms (`ncclCommSplit`); lift the daemon `pp>1 && tp>1` refusal. | 2×2 (emulated) + hiptrx 4× gfx1201 + **hipx mixed-arch ragged (gfx1151+gfx1010)** coherence |
| **7** *(closeout — initiate follow-ups)* | **Explicitly open the deferred tracks** (below) as tracked work so they're initiated, not silently dropped: file an issue + `NEXT-STEPS.md` entry per track, each carrying scope + first concrete step + which core phase it depends on. | issues filed + `NEXT-STEPS.md` updated |

**Forward-reach vs serve-reach (the load-bearing distinction).** Making an arch's *forward* mesh-capable (Phases 1–2) is NOT the same as making it *serve-reachable*: the daemon's multi-GPU serve path is the god-struct — `EpArch` enum (`daemon.rs:1102`, only `Ds4|Minimax`), `generate_ep` (matches only those), and `load_model_pp` **hard-rejects `arch_id ∉ {5,6}`** (`daemon.rs:4843`). So each arch-reach claim splits: **forward-reach** = Phase 1+2; **serve-reach** = needs either a *temporary* `EpArch`/`load_model_pp` allowlist extension, or Phase 3's `ArchDispatch`. Concretely: **Phase 1c** (llama PP) must lift the 4843 guard + add a llama serve path; **Phase 3**'s "all EP arches" gate must *add* an `EpArch::Qwen35` variant (qwen35 has `forward_ep` at the arch level but no serve variant); **Phase 4** (qwen2) needs an `EpArch::Qwen2` variant *or* Phase 3 — its "depends on 1+2 not 3" holds for forward-reach only. Bake this into each phase's exit criteria.

**Dependency notes:** Phase 0 (hardware extraction) is a foundational mechanical prerequisite for the one-crate executor. 1a is a breaking cross-crate trait change (atomic). 1b needs the built PP oracle (not the single-GPU one). 1c depends only on 1 and de-risks 2–3. Phase 4 arch migrations depend on 1+2, **not** 3. Phase 6 (unspeakable-collectives) depends on Phase 0's crate firewall already being in place. **Phase 3 fallback:** if EP-rehome is bounced/stalls, the god-struct stays as a compat layer and Phases 1–2+4 still ship multi-arch PP.

Safety lands incrementally: manifest diff (P0), shape-dry-run + NaN-poison (P1b), epoch stamp (P3).

---

## Critical files

- **NEW leaf crate `hipfire-hardware`:** receives `crates/hipfire-runtime/src/multi_gpu.rs` verbatim (`Gpus` + all collectives + device resolution), deps `hip-bridge` + `rdna-compute` only; device-resolution config reads become a `DeviceResolveOpts` param. `hipfire-dispatch` gains a dep on it.
- **Executor + EP arm (in dispatch):** `crates/hipfire-dispatch/src/pipeline/` (new `parallel_exec.rs`; absorb `hipfire-runtime/src/ep.rs::run_layer_program_ep`). Lives in dispatch because Phase 0 made `Gpus` reachable there without a cycle.
- **IR + trait + single-GPU exec:** `crates/hipfire-dispatch/src/pipeline/superop.rs` (`SuperOp`/`SuperOpKind`/`ForwardBindings`/`dispatch_super_op`/`run_layer_program`) — add `CollectiveHint`, `OpOutput`, `n_rows`, `band_xfer` on `LoweredForward`. (Note: the module doc claiming lowered is "default off" is **stale** — it's default-ON/live; don't trust it.)
- **Placement + collectives (moves to hardware, then reused):** `multi_gpu.rs` `Gpus`: `boundary_copy`/`wait_boundary`, `all_reduce_sum_f32[_peer]`, `device_for_layer`/`band_starts`/`output_device`, `init_uniform`/`init_layers`/`init_tp`, `HIPFIRE_EMULATE_GPUS` aliasing.
- **Sharding math (reuse, tested):** `crates/hipfire-runtime/src/tp_shard.rs` (`ShardConfig`, `owns_expert`, `q_head_range`, `dn_value_head_range`, `wo_col_range`, `ExpertAssign`).
- **State ctors to collapse:** `crates/hipfire-runtime/src/llama.rs` (~15 `KvCache::*_multi` + `alloc_kv_per_layer_multi*`), `crates/hipfire-arch-qwen35/src/qwen35.rs` (`DeltaNetState::new_with_quant_multi` + `la_to_device`), `crates/hipfire-arch-lfm2moe/src/lfm2moe.rs` (conv-state, no multi path).
- **Arch trait:** `crates/hipfire-runtime/src/arch.rs` (add `weight_manifest`/`state_manifest`).
- **Loader/routing + god-struct + refusal contracts (two sites):** `crates/hipfire-runtime/examples/daemon.rs` (`load_model` ~3305, `load_model_pp` ~4827 [arch 5/6 + VL refusal], `load_model_ep`/`_minimax` ~5135/5256 [arch 9/10], `LoadedModel` ~1118, mutual-exclusion + spec refusals ~1858-1926) + `resolve_parallelism`→`resolve_mesh` (`config.rs`). *(Unified-loading branches carry this as `hipfire-loader`/Carrier — converge with that.)*
- **Bespoke PP/EP to delete:** qwen35 `forward_scratch_layers_multi`, `forward_prefill_batch_multi`, `forward_prefill_batch_ep`, `load_weights_multi`.
- **EP hook pattern to reuse:** ds4 `crates/hipfire-arch-deepseek4/src/{arch.rs,forward.rs}` (`load_weights_sharded`, zeroed-dummy, `run_moe_ep`/`ep_add_into_residual`); minimax mirror.

## Transparency boundary (what's generic vs genuinely arch-specific)
- **FusedQKV under GQA** and **DeltaNet `w_alpha/w_beta/wz`**: **NOT irreducible** — first-class head-axis policies (`FusedQKV{...,layout}` / `HeadSharded`) backed by `tp_shard`'s existing (dead) head-range math, declared at load time → forward stays transparent. Greenfield (never wired to a forward) + DeltaNet needs `s_ef_residual` wired (qwen35.rs:1091); real Phase-5 work, but declarative.
- **`ExpertSharded`** is the one genuinely arch-specific policy: MoE packed-blob + pointer-table + zeroed-dummy convention → rich policy with a per-arch descriptor, not a generic slice.
- **`Custom{shard_fn}`** — last-resort opt-out; **no known example in the current fleet** after the above.
- **`WeightPager` × mesh** — explicitly deferred/out-of-scope (see §4): inert single-device scaffolding that current multi-GPU code asserts against; revisit only if >VRAM-across-mesh is needed (UMA lowers priority).

## Verification
- **No-GPU / CI:** unit tests — `validate_rank_programs` (mismatched `LayerProgram`s → `Err` at right layer/op), `weight_manifest`/`state_manifest` for ds4/minimax/qwen35 (shape+policy), shard-axis inference, `resolve_mesh` (pp/tp/emulate → expected tree incl. 2×2). `cargo build --release --workspace` + `cargo test --lib`.
- **Byte-exact oracles:** single-GPU is covered by the **real** external `HIPFIRE_FORWARD_LOWERED=0` vs `=1` committed-token md5 A/B (default-on lowered). *(The in-process `HIPFIRE_FORWARD_ORACLE` referenced in a superop.rs comment does NOT exist — do not rely on it.)* **PP has no oracle today — Phase 1b must build one** (hand loop `=0` vs generic `=1`).
- **Single-card multi-rank:** `HIPFIRE_EMULATE_GPUS=2` (built + validated this session) exercises PP (`+HIPFIRE_PP=2` **via the Bun CLI**, which plumbs `HIPFIRE_PP→params.pp`; the daemon itself has no `HIPFIRE_PP` env) and EP (default). **Phase 5a extends it for heterogeneity** — `HIPFIRE_EMULATE_ARCHS` (per-vGPU downlevel-compatible arch, e.g. gfx1100 on a gfx1151 card) + `HIPFIRE_EMULATE_VRAM` (per-vGPU asymmetric memory budget) — so mixed-arch (per-arch lowering, mixed-arch preflight) and ragged/VRAM-weighted placement are testable on the dev box; only RDNA4-specific codegen still needs hiptrx. **Verify early:** that emulated EP actually exercises the `all_reduce_sum_f32_peer` path end-to-end (it should — aliased `[0,0]` makes peer copies same-device d2d + local add — but this session only confirmed EP *construction*, not a completed EP generation). If emulation doesn't cover the reduce, Phase 2 EP validation needs real multi-GPU HW.
- **Gates:** `./scripts/coherence-gate.sh` (forward-pass) + `./scripts/serve-multiturn-gate.sh` (the mandatory Phase 3 EP-rehome / state-bleed gate). `scripts/probe_commits.sh` for the EP-prefill collective cost.

## Scope statements (explicit)
- **Spec-decode (DFlash/MTP/CASK/PFlash):** the unified executor drives the **target forward only**. Drafter-device placement is owned by the separate hetero-pflash-dflash PRD; today `pp>1`/`tp>1` refuse drafters (daemon), and those **refusals are retained** until that PRD lands. This plan must not fight it over `daemon.rs:1858-1926` / `generate_multi`.
- **VL/multimodal (dots-ocr, qwen35-vl):** vision towers are bespoke hand-forwards that produce no `LayerProgram` and are refused under parallelism. **Out of scope**; arch reach targets text arches (llama, qwen2).
- **TP vs EP:** today `tp>1` in routing == EP (expert shard). Real row/column TP is greenfield (Phase 5); when it lands the `tp>1→EP` routing must be disambiguated.
- **cohere2moe (arch 12):** ~~absent in this worktree — dropped from arch-reach claims until the crate lands.~~ **SUPERSEDED (2026-07-06 pivot):** the `crates/hipfire-arch-cohere2moe` crate IS present post-merge and calls `execute_steps` directly (`forward.rs:267,748`) — it's a P-A driver (`decode_step_body`), in scope.

## Follow-up tracks (each initiated at Phase 7 — issue + `NEXT-STEPS.md` entry, not silently deferred)
Once the mesh spine (Phases 0–5b) lands, these are the axes deliberately out of the core plan. Phase 7's job is to *open* them, each with the first step below:
- **Spec-decode × mesh** (DFlash/MTP/CASK/PFlash under PP/TP/EP). Core plan drives the **target forward only**; drafters are refused today (daemon `pp>1`/`tp>1`) and drafter-device placement is owned by the separate hetero-pflash-dflash PRD. *First step:* reconcile with that PRD — does the drafter become a leaf/coordinate in the mesh (`ModelParallel` owns its device), or stay a side-channel? Depends on Phase 3 (`ModelParallel`).
- **VL/multimodal × mesh** (dots-ocr, qwen35-vl). Vision towers are bespoke hand-forwards producing no `LayerProgram`, refused under parallelism today. *First step:* lower a vision tower onto `LayerProgram`/`ForwardBindings` (a non-causal, no-KV forward — the IR's decode/prefill assumptions don't fit cleanly; may need a third IR shape) + remove the VL parallelism refusal. Depends on Phases 1–2.
- **True TP beyond EP-aliasing** (row/col tensor-parallel + eventual sequence-parallel). Phase 5 starts real row/col TP; this track carries the remainder — disambiguating `tp>1→EP` routing fleet-wide, TP for the arches not yet on `ForwardBindings`, and sequence-parallel as a fourth `Dimension` kind if warranted. *First step:* the `tp>1` routing disambiguation (Phase 5 leaves it half-done). Depends on Phase 5.

## Deferred / rejected
- Async collective/compute overlap (double-buffered partials) — perf, single-thread-safe, after correctness.
- **Rejected: adaptive/emergent placement** (starvation-cut PP, stigmergy TP-steal, pheromone expert migration) — incompatible with byte-exact/coherence determinism. A *static* profile-derived PP band-cut is the only survivor, optional.
- JIT weight streaming (>VRAM) — orthogonal; UMA lowers priority.
- Training/backward placement reuse — out of inference scope.
